# M6d Goal-Mode Current Anchor

> **2026-07-15 active boundary:** W2c ended with 0/8 threshold candidates, and its distinct 58-case W3
> AF2-Multimer mechanism panel is complete. Corrected job `3084977` completed 58/58 after job `3084976`
> was cancelled and invalidated for target-MSA truncation. Frozen adjudication supports Chai on 3PC8,
> finds mixed W2c agreement with Boltz, and returns `context_dependent_or_unresolved`. Resume from
> `docs/M6D_W3_MECHANISM_PANEL_COMPLETION.md`; do not rescue W2c or retune W3 from these outcomes. The
> W3b successor is prospectively locked with eight label-blind fresh target roles, exact endpoint power
> 0.824333, a frozen matched-predictor evaluator, and an exact dual-predictor runtime lock. The separately
> approved target-MSA stage completed 8/8 at 0.216389 A40 GPU-hours, strict replay passed, and the immutable
> 870-slot execution lock now exists. The fit approval packet and zero-submit dry-run are ready, but no fit
> candidate generation or predictor compute is approved.

Date: 2026-07-15

This is the current long-running Codex goal-mode anchor for `bio_sfm_designer`.
It is a project-development anchor, not a publication plan.

Latest local harness refresh: 2026-07-15.

Latest scientific override: 2026-07-11. W2 remains negative as a universal/generalized gate. W2b
certification completed on five fit-eligible targets with 300 fresh H100 records and strict QC failures=0.
Four `trust_all` targets certified, but the sole `selective_pae` target failed exact certification. The
locked panel requires one selective certificate, so W2b v1 is terminally not supported and no test compute
was submitted. The authoritative result artifact is `docs/M6D_W2B_CERTIFICATION_COMPLETION.md`.

Latest successor override: 2026-07-14. W2c is a new selective-pAE-only one-shot
experiment with an exact prospective power gate. Its design qualifies at 90 certification
accepts with conditional power 0.817860 under design risk 0.08. Its locked evaluator is
implemented, and eight label-blind fresh targets are now selected with zero historical/W2b target,
source, or sequence overlap. Target-MSA preparation is complete for all eight targets, strict manifest
validation passes 8/8, and total A40 allocation was 0.144722 GPU-hours. The separately approved
`w2c-fit-learn-v1` stage then completed with 480 H100-generated Boltz records, 480/480 strict-QC rows,
16/16 local-to-Cayuga file-hash matches, no retries, and 1.0775 H100 GPU-hours. All eight target rules
froze to `refuse`; zero selective candidates remain against the required minimum of three. W2c is now
`w2c_threshold_learning_terminal_not_supported`, and no independent-screen or certification compute is
approved. The authoritative result is `docs/M6D_W2C_THRESHOLD_LEARNING_COMPLETION.md`. The W3 successor
then completed under its frozen 58-case protocol. The 3PC8 block supports Chai (`12/12` discordant labels,
`6/6` controls), while the W2c block is mixed (`30/40` Boltz agreement; `5/8` targets at least `4/5`).
The joint result is `context_dependent_or_unresolved`. All W2 v1-v11 execution entries later in this
chronological ledger are historical even where their original labels say `current`. W3b is now
`w3b_fit_packet_ready_awaiting_explicit_approval`: eight unused targets are locked to 3 fit,
3 certification, and 2 held-out-test roles; their MSAs, strict lifecycle, and execution lock are complete.

## Active Objective

Continue the M6d science-result program in Cayuga-first goal mode:

- preserve the terminal one-shot W2c selective-pAE result without post-hoc rescue;
- preserve the completed frozen 58-case W3 result without changing its inputs, thresholds, or adjudication;
- preserve the preregistered W3b disagreement-aware gate, target roles, exact-power design, and staged
  stop rules before any new predictor output is generated;
- preserve W1 as target-specific certified evidence;
- preserve W4 as closed-loop plumbing evidence;
- keep all status artifacts, tests, and local/Cayuga handoff anchors honest and reproducible.

## Current Evidence Boundary

- W1: certified as target-specific complex evidence.
- W2: not certified as universal multi-target generalization. W2b v1 is also terminally not supported:
  four `trust_all` targets certified, but zero selective-pAE targets certified against a requirement of one.
- W2c: threshold learning completed on eight fresh targets with 480/480 strict-QC rows. All eight targets
  refused under the locked rule, so selective target-adaptive viability is terminally unsupported. No
  independent-screen or certification compute is approved or scientifically reachable.
- W3: the bounded third-predictor mechanism panel is complete and audited. 3PC8 supports Chai over Boltz,
  but W2c agreement is target-dependent and mixed; the joint outcome is `context_dependent_or_unresolved`.
  Population-level independent-predictor robustness remains unsupported.
- W3b: the fresh matched-predictor gate is preregistered and power-qualified. The consumed MSA-only packet
  submitted jobs `3085384`-`3085391`; all completed `0:0` without retry at 0.216389 A40 GPU-hours. The 56
  scoped artifacts pass strict 8/8 manifest, report, and frozen-sequence checks. Cayuga's raw `AllocTRES`
  omitted the subtype, so a versioned reconciliation accepted A40 only after 8/8 submit-time `scontrol`
  proof, terminal one-GPU accounting, A40 node inventory, and the packet-bound A40 sbatch all agreed; it
  cannot suppress any non-telemetry failure. The resulting lifecycle is
  `target_msa_precompute_complete_8_of_8`, and the verified execution lock binds eight MSA hashes and 870
  design slots. Boltz `2.2.1` and AF2-Multimer/ColabFold `1.6.1` remain frozen by exact package/container/
  checkpoint/weight hashes. The downstream fit packet is emitted and its guarded dry-run proves exactly
  3 CPU plus 6 H100 jobs, 180 candidates, and 360 matched evaluations with zero scheduler or receipt
  writes. No fit approval, candidate generation, predictor run, gate claim, or biological-success claim
  exists.
- W4: closed-loop plumbing is complete, but it is fail-closed/all-defer evidence rather than
  productive build-selection evidence.

Do not pool target evidence into a generalization claim. Do not close the single-predictor caveat
from Chai records alone.

## Current Artifacts

- Project status: `results/m6c_project_status_w2_followup.json`
- W2b fit completion: `docs/M6D_W2B_FIT_COMPLETION.md`
- W2b CPU-replay fixture: `tests/fixtures/m6d_w2b_target_adaptive_fit_records.jsonl`
- W2b fit report: `results/m6d_w2b_target_adaptive_fit_report.json`
- W2b certification completion: `docs/M6D_W2B_CERTIFICATION_COMPLETION.md`
- W2b certification replay fixture: `tests/fixtures/m6d_w2b_target_adaptive_certification_records.jsonl`
- W2b terminal certification report: `results/m6d_w2b_target_adaptive_certification_report.json`
- W2b certification output manifest: `results/m6d_w2b_target_adaptive_certification_output_manifest.json`
- W2c one-shot protocol: `docs/M6D_W2C_ONE_SHOT_PROTOCOL.md` and
  `configs/m6d_w2c_one_shot_protocol.json`
- W2c no-submit design gate: `results/m6d_w2c_design_gate.{json,md}`
- W2c fresh target manifest and label-blind selection:
  `configs/m6d_w2c_fresh_targets.json` and `results/m6d_w2c_target_selection.json`
- Historical immutable W2c target-MSA approval packet:
  `docs/M6D_W2C_TARGET_MSA_APPROVAL.md` and
  `results/m6d_w2c_target_msa_approval_packet.json`
- W2c target-MSA completion: `results/m6d_w2c_target_msa_completion.json`
- W2c threshold-learning stage manifest and input lock:
  `configs/m6d_w2c_fit_learn_targets.json` and `configs/m6d_w2c_fit_learn_input_lock.json`
- Historical consumed W2c threshold-learning approval boundary:
  `docs/M6D_W2C_FIT_LEARN_APPROVAL.md`
- W2c threshold-learning completion:
  `docs/M6D_W2C_THRESHOLD_LEARNING_COMPLETION.md`, plus ignored local operational artifacts
  `results/m6d_w2c_threshold_learning_report.json` and `results/m6d_w2c_fit_learn_completion.json`
- W3 decisive mechanism-panel protocol and rationale:
  `configs/m6d_w3_mechanism_panel_protocol.json` and `docs/M6D_W3_MECHANISM_PANEL.md`
- W3 completion and public CPU replay:
  `docs/M6D_W3_MECHANISM_PANEL_COMPLETION.md` and
  `tests/fixtures/m6d_w3_mechanism_panel_af2_records.jsonl`
- W3b no-submit protocol, target roles, and frozen evaluator:
  `docs/M6D_W3B_DISAGREEMENT_GATE_PROTOCOL.md`,
  `configs/m6d_w3b_disagreement_gate_protocol.json`, and
  `configs/m6d_w3b_fresh_targets.json`
- W3b target-MSA approval boundary:
  `docs/M6D_W3B_TARGET_MSA_APPROVAL.md` and
  `results/m6d_w3b_target_msa_approval_packet.json`
- W3b target-MSA completion and allocation reconciliation:
  `docs/M6D_W3B_TARGET_MSA_COMPLETION.md`,
  `results/m6d_w3b_target_msa_lifecycle.{json,md}`, and
  `bio_sfm_designer.experiments.m6d_w3b_target_msa_allocation_reconcile`
- W3b Cayuga no-submit readiness evidence:
  `results/m6d_w3b_target_msa_remote_readiness.{json,md}`
- W3b target-MSA lifecycle and post-submit replay:
  `results/m6d_w3b_target_msa_lifecycle.{json,md}`,
  `results/m6d_w3b_target_msa_job_state_query.sh`, and
  `results/m6d_w3b_target_msa_sync_back.sh`
- W3b lifecycle-derived execution-lock readiness:
  `results/m6d_w3b_execution_lock_readiness.{json,md}` and
  `bio_sfm_designer.experiments.m6d_w3b_execution_lock`
- W3b immutable execution manifest/input lock:
  `configs/m6d_w3b_execution_targets.json` and `configs/m6d_w3b_execution_input_lock.json`
- W3b exact dual-predictor runtime lock:
  `configs/m6d_w3b_runtime_lock.json`, `results/m6d_w3b_runtime_lock_readiness.{json,md}`, and
  `bio_sfm_designer.experiments.m6d_w3b_runtime_lock`
- W3b provenance-bound matched-record contract:
  `results/m6d_w3b_matched_record_contract.{json,md}` and
  `bio_sfm_designer.experiments.m6d_w3b_matched_records`
- W3b dedicated fit producer and approval boundary:
  `docs/M6D_W3B_FIT_APPROVAL.md`, `results/m6d_w3b_fit_packet_readiness.{json,md}`,
  `bio_sfm_designer.experiments.m6d_w3b_fit_packet`, and `hpc/run_w3b_fit_guarded.sh`
- W3 guarded execution, runtime receipt, conversion, and adjudication code:
  `hpc/run_w3_mechanism_panel_guarded.sh`, `hpc/validate_w3_mechanism_runtime.sh`,
  `hpc/convert_colabfold_mechanism_panel.py`, and
  `src/bio_sfm_designer/experiments/m6d_w3_mechanism_adjudication.py`
- Current tracked goal-state and ranked science-action anchor:
  `results/m6d_goal_state_refresh_report.{json,md}`
- Local extended science-action ledger, when present:
  `results/m6d_followup_next_science_actions.{json,md}`
- Compact tracked machine-readable goal state: `results/m6d_goal_state_refresh_report.json`
- Local extended machine-readable goal anchor, when present:
  `results/m6d_goal_mode_current_anchor.json`
- Historical pre-AF2 W2/W3 decision protocol: `results/m6d_w2_w3_decision_protocol.{json,md}`
- Current W3 next-protocol contract: `results/m6d_w3_next_protocol.{json,md}`
- Current W3 challenge manifest: `results/m6d_w3_challenge_manifest.{json,md}`
- Current W3 third-predictor execution contract: `results/m6d_w3_third_predictor_contract.{json,md}`
- Current W3 predictor/protocol selection card: `results/m6d_w3_predictor_selection_card.{json,md}`
- Current W3 runtime-probe plan: `results/m6d_w3_runtime_probe_plan.{json,md}`
- Current W3 runtime-probe report: `results/m6d_w3_runtime_probe_report.{json,md}`
- Current W3 local-static runtime-probe snapshot:
  `results/m6d_w3_runtime_probe_report_local_static.{json,md}`
- Current W3 Cayuga runtime-probe packet: `results/m6d_w3_cayuga_runtime_probe_packet.{json,md}`
  and `results/m6d_w3_cayuga_runtime_probe_no_submit.sh`
- Current W3 Cayuga observed runtime checks:
  `results/m6d_w3_cayuga_runtime_probe_observed_checks.json`
- Current W3 Cayuga runtime repair discovery:
  `results/m6d_w3_cayuga_runtime_repair_discovery.json`
- Current W3 runtime repair plan:
  `results/m6d_w3_runtime_repair_plan.{json,md}`
- Current W3 runtime provision packet:
  `results/m6d_w3_runtime_provision_packet.{json,md}` and
  `results/m6d_w3_runtime_provision_colabfold_guarded.sh`
- Current local harness status: `results/m6d_goal_mode_local_harness_status.{json,md}`
- Current active W2 v11 no-spend follow-up contract:
  `results/m6d_w2_target_family_redesign_v11_followup_contract.{json,md}`
- Current active W2 v11 candidate rules:
  `configs/m6d_w2_target_family_redesign_v11_candidate_rules.json`
- Current active W2 v11 panel submission-decision state:
  `results/m6d_w2_target_family_redesign_v11_submission_decision_state.{json,md}`
- Current W2 v10 full-panel completion/report/diagnostic:
  `results/m6d_w2_target_family_redesign_v10_panel_completion.json`,
  `results/m6d_w2_target_family_redesign_v10_panel_report_alpha02.json`,
  `results/m6d_w2_target_family_redesign_v10_panel_report_alpha03.json`, and
  `results/m6d_w2_target_family_redesign_v10_redesign_diagnostic_alpha02.{json,md}`
- Historical W2 v9 target-MSA execution attempt:
  `results/m6d_w2_target_family_redesign_v9_full14_target_msa_execution_attempt.{json,md}`
- Historical W2 v9 panel preflight:
  `results/m6d_w2_target_family_redesign_v9_panel_preflight.{json,md}`
- Historical W2 v9 panel wrapper guard:
  `results/m6d_w2_target_family_redesign_v9_panel_wrapper_guard_audit.{json,md}`
- Historical W2 v9 panel approval packet:
  `results/m6d_w2_target_family_redesign_v9_panel_approval_packet.{json,md}`
- Historical W2 v9 post-panel decision protocol:
  `results/m6d_w2_target_family_redesign_v9_panel_decision_protocol.{json,md}`
- Historical W2 v9 panel report:
  `results/m6d_w2_target_family_redesign_v9_panel_report.json`
- Historical W2 v9 post-panel redesign diagnostic:
  `results/m6d_w2_target_family_redesign_v9_redesign_diagnostic.{json,md}`
- Historical W2 v9 gate strategy:
  `results/m6d_w2_target_family_redesign_v9_gate_strategy.{json,md}`
- Current W2 v10 follow-up contract:
  `results/m6d_w2_target_family_redesign_v10_followup_contract.{json,md}`
- Current W2 v10 candidate rules:
  `configs/m6d_w2_target_family_redesign_v10_candidate_rules.json`
- Current W2 v10 seed expansion:
  `results/m6d_w2_target_family_redesign_v10_seed_expansion.{json,md}` and
  `configs/m6d_w2_target_family_redesign_v10_seed_rcsb_ids.json`
- Current W2 v10 discovery pool:
  `results/m6d_w2_target_family_redesign_v10_discovery_pool.{json,md}` and
  `configs/m6d_w2_target_family_redesign_v10_discovery_targets.json`
- Current W2 v10 sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v10_sequence_diversity.{json,md}`
- Current W2 v10 representative manifest:
  `configs/m6d_w2_target_family_redesign_v10_representative_targets.json`
- Current W2 v10 pre-MSA manifest and MSA plan:
  `results/m6d_w2_target_family_redesign_v10_manifest_pre_msa.json` and
  `results/m6d_w2_target_family_redesign_v10_target_msas.sh`
- Current W2 v10 pre-sync input-prep completion:
  `results/m6d_w2_target_family_redesign_v10_input_prep_completion_pre_sync.{json,sh}` and
  `results/m6d_w2_target_family_redesign_v10_pending_input_prep_paths.txt`
- Current W2 revised branch: `results/m6d_w2_revised_branch.{json,md}`
- Current W2 candidate-pool screen: `results/m6d_w2_candidate_pool_screen.{json,md}`
- Current W2 next-branch design: `results/m6d_w2_next_branch_design.{json,md}`
- Current W2 next-branch candidate rules: `configs/m6d_w2_next_branch_candidate_rules.json`
- Current W2 next-branch candidate pool: `results/m6d_w2_next_branch_candidate_pool.{json,md}`
- Current W2 next-branch manifest design: `results/m6d_w2_next_branch_manifest_design.{json,md}`
- Current W2 expanded next-branch manifest:
  `configs/m6d_w2_expanded_next_branch_targets.json`
- Current W2 expanded next-branch strict preflight:
  `results/m6d_w2_expanded_next_branch_manifest.json`
- Current W2 expanded next-branch submit plan:
  `results/m6d_w2_expanded_next_branch_submit_plan.sh`
- Current W2 expanded next-branch receipt-preserving submit wrapper:
  `results/m6d_w2_expanded_next_branch_submit_with_receipt.sh`
- Current W2 expanded next-branch submit-wrapper validation:
  `results/m6d_w2_expanded_next_branch_submit_wrapper_validation.json`
- Current W2 expanded next-branch submission status:
  `results/m6d_w2_expanded_next_branch_submission_status.json`
- Current W2 expanded next-branch completion replay:
  `results/m6d_w2_expanded_next_branch_completion.{json,sh}`
- Current W2 expanded next-branch records sync-back/replay plan:
  `results/m6d_w2_expanded_next_branch_sync_back.sh`
- Current W2 target-family representative completion:
  `results/m6d_w2_target_family_redesign_v1_rcsb_representative_completion.json`
- Current W2 target-family representative panel report:
  `results/m6d_w2_target_family_redesign_v1_rcsb_representative_panel_report.json`
- Current W2 target-family representative diagnostic:
  `results/m6d_w2_target_family_redesign_v1_rcsb_representative_redesign_diagnostic.{json,md}`
- Current W2 target-family v2 design:
  `results/m6d_w2_target_family_redesign_v2_design.{json,md}`
- Current W2 target-family v2 candidate rules:
  `configs/m6d_w2_target_family_redesign_v2_candidate_rules.json`
- Current W2 target-family v2 candidate-pool screen:
  `results/m6d_w2_target_family_redesign_v2_candidate_pool.{json,md}`
- Current W2 target-family v2 RCSB seed expansion:
  `results/m6d_w2_target_family_redesign_v2_rcsb_seed_expansion.{json,md}`
- Current W2 target-family v2 RCSB discovery pool:
  `results/m6d_w2_target_family_redesign_v2_rcsb_discovery_pool.{json,md}`
- Current W2 target-family v2 RCSB discovery manifest:
  `configs/m6d_w2_target_family_redesign_v2_rcsb_discovery_targets.json`
- Current W2 target-family v2 RCSB sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v2_rcsb_sequence_diversity.{json,md}`
- Current W2 target-family v2 RCSB post-MSA strict preflight:
  `results/m6d_w2_target_family_redesign_v2_rcsb_discovery_targets_manifest_post_msa_require_files.json`
- Current W2 target-family v2 RCSB submit plan:
  `results/m6d_w2_target_family_redesign_v2_rcsb_submit_plan.sh`
- Current W2 target-family v2 RCSB receipt-preserving submit wrapper:
  `results/m6d_w2_target_family_redesign_v2_rcsb_submit_with_receipt.sh`
- Current W2 target-family v2 RCSB submit receipt:
  `results/m6d_w2_target_family_redesign_v2_rcsb_submit_receipt.jsonl`
- Current W2 target-family v2 RCSB sync-back plan:
  `results/m6d_w2_target_family_redesign_v2_rcsb_sync_back.sh`
- Current W2 target-family v2 RCSB completion plan:
  `results/m6d_w2_target_family_redesign_v2_rcsb_completion.sh`
- Current W2 target-family v2 RCSB completion:
  `results/m6d_w2_target_family_redesign_v2_rcsb_completion.json`
- Current W2 target-family v2 RCSB panel report:
  `results/m6d_w2_target_family_redesign_v2_rcsb_panel_report.json`
- Current W2 target-family v2 RCSB redesign diagnostic:
  `results/m6d_w2_target_family_redesign_v2_rcsb_redesign_diagnostic.{json,md}`
- Current W2 target-family v3 design:
  `results/m6d_w2_target_family_redesign_v3_design.{json,md}`
- Current W2 target-family v3 candidate rules:
  `configs/m6d_w2_target_family_redesign_v3_candidate_rules.json`
- Current W2 target-family v3 RCSB seed expansion:
  `results/m6d_w2_target_family_redesign_v3_rcsb_seed_expansion.{json,md}`
- Current W2 target-family v3 discovery pool:
  `results/m6d_w2_target_family_redesign_v3_discovery_pool.{json,md}`
- Current W2 target-family v3 representative manifest:
  `configs/m6d_w2_target_family_redesign_v3_representative_targets.json`
- Current W2 target-family v3 sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v3_sequence_diversity.{json,md}`
- Current W2 target-family v3 pre-MSA manifest report:
  `results/m6d_w2_target_family_redesign_v3_manifest_pre_msa.json`
- Current W2 target-family v3 target-MSA plan:
  `results/m6d_w2_target_family_redesign_v3_target_msas.sh`
- Current W2 target-family v3 target-MSA sync-back/replay plan:
  `results/m6d_w2_target_family_redesign_v3_msa_sync_back.sh`
- Current W2 target-family v3 completion:
  `results/m6d_w2_target_family_redesign_v3_completion.json`
- Current W2 target-family v3 panel report:
  `results/m6d_w2_target_family_redesign_v3_panel_report.json`
- Current W2 target-family v3 redesign diagnostic:
  `results/m6d_w2_target_family_redesign_v3_redesign_diagnostic.{json,md}`
- Current W2 target-family v4 design:
  `results/m6d_w2_target_family_redesign_v4_design.{json,md}`
- Current W2 target-family v4 candidate rules:
  `configs/m6d_w2_target_family_redesign_v4_candidate_rules.json`
- Current W2 target-family v4 candidate-pool screen:
  `results/m6d_w2_target_family_redesign_v4_candidate_pool.{json,md}`
- Current W2 target-family v4 RCSB seed expansion:
  `results/m6d_w2_target_family_redesign_v4_rcsb_seed_expansion.{json,md}`
- Current W2 target-family v4 discovery pool:
  `results/m6d_w2_target_family_redesign_v4_discovery_pool.{json,md}`
- Current W2 target-family v4 representative manifest:
  `configs/m6d_w2_target_family_redesign_v4_representative_targets.json`
- Current W2 target-family v4 sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v4_sequence_diversity.{json,md}`
- Current W2 target-family v4 post-MSA strict preflight:
  `results/m6d_w2_target_family_redesign_v4_manifest_post_msa_require_files.json`
- Current W2 target-family v4 submit receipt:
  `results/m6d_w2_target_family_redesign_v4_submit_receipt.jsonl`
- Current W2 target-family v4 submit receipt summary:
  `results/m6d_w2_target_family_redesign_v4_submit_receipt_summary.json`
- Current W2 target-family v4 completion:
  `results/m6d_w2_target_family_redesign_v4_completion.json`
- Current W2 target-family v4 panel report:
  `results/m6d_w2_target_family_redesign_v4_panel_report.json`
- Current W2 target-family v4 redesign diagnostic:
  `results/m6d_w2_target_family_redesign_v4_redesign_diagnostic.{json,md}`
- Current W2 target-family v5 design:
  `results/m6d_w2_target_family_redesign_v5_design.{json,md}`
- Current W2 target-family v5 candidate rules:
  `configs/m6d_w2_target_family_redesign_v5_candidate_rules.json`
- Current W2 target-family v5 ready candidate-pool screen:
  `results/m6d_w2_target_family_redesign_v5_candidate_pool_ready.{json,md}`
- Current W2 target-family v5 manifest:
  `configs/m6d_w2_target_family_redesign_v5_targets.json`
- Current W2 target-family v5 manifest design:
  `results/m6d_w2_target_family_redesign_v5_manifest_design.{json,md}`
- Current W2 target-family v5 pre-submit strict preflight:
  `results/m6d_w2_target_family_redesign_v5_manifest_pre_submit.json`
- Current W2 target-family v5 submit receipt summary:
  `results/m6d_w2_target_family_redesign_v5_submit_receipt_summary.json`
- Current W2 target-family v5 completion:
  `results/m6d_w2_target_family_redesign_v5_completion.json`
- Current W2 target-family v5 panel report:
  `results/m6d_w2_target_family_redesign_v5_panel_report.json`
- Current W2 target-family v5 redesign diagnostic:
  `results/m6d_w2_target_family_redesign_v5_redesign_diagnostic.{json,md}`
- Current W2 target-family v5 sync-back/replay plan:
  `results/m6d_w2_target_family_redesign_v5_sync_back.sh`
- Current W2 target-family v5 gate strategy:
  `results/m6d_w2_target_family_redesign_v5_gate_strategy.{json,md}`
- Current W2 target-family v6 design:
  `results/m6d_w2_target_family_redesign_v6_design.{json,md}`
- Current W2 target-family v6 candidate rules:
  `configs/m6d_w2_target_family_redesign_v6_candidate_rules.json`
- Current W2 target-family v6 candidate-pool screen:
  `results/m6d_w2_target_family_redesign_v6_candidate_pool.{json,md}`
- Current W2 target-family v6 gate-strategy resolution:
  `results/m6d_w2_target_family_redesign_v6_gate_strategy_resolution.{json,md}`
- Current W2 target-family v7 protocol:
  `configs/m6d_w2_target_family_redesign_v7_protocol.json`
- Current W2 target-family v7 seed expansion:
  `results/m6d_w2_target_family_redesign_v7_seed_expansion.{json,md}`
- Current W2 target-family v7 discovery pool:
  `results/m6d_w2_target_family_redesign_v7_discovery_pool.{json,md}`
- Current W2 target-family v7 sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v7_sequence_diversity.{json,md}`
- Current W2 target-family v7 representative manifest:
  `configs/m6d_w2_target_family_redesign_v7_representative_targets.json`
- Current W2 target-family v7 representative sequence-diversity audit:
  `results/m6d_w2_target_family_redesign_v7_representative_sequence_diversity.{json,md}`
- Current W2 target-family v7 representative pre-MSA manifest report:
  `results/m6d_w2_target_family_redesign_v7_representative_manifest_pre_msa.json`
- Current W2 target-family v7 target-MSA plan:
  `results/m6d_w2_target_family_redesign_v7_target_msas.sh`
- Current W2 target-family v7 target-MSA receipt:
  `results/m6d_w2_target_family_redesign_v7_target_msa_precompute_receipt.jsonl`
- Current W2 target-family v7 MSA sync-back/preflight plan:
  `results/m6d_w2_target_family_redesign_v7_msa_sync_back.sh`
- Current W2 target-family v7 submit wrapper:
  `results/m6d_w2_target_family_redesign_v7_submit_with_receipt.sh`
- Current W2 target-family v7 submit receipt:
  `results/m6d_w2_target_family_redesign_v7_submit_receipt.jsonl`
- Current W2 target-family v7 records sync-back/completion plan:
  `results/m6d_w2_target_family_redesign_v7_sync_back.sh`
- Current W2 source-redundancy audit plan: `results/m6d_w2_source_redundancy_audit_plan.{json,md}`
- Current W2 expanded-discovery seed config:
  `configs/m6d_w2_expanded_discovery_seed_rcsb_ids.json`
- Current W2 expanded-discovery pool: `results/m6d_w2_expanded_discovery_pool.{json,md}`
- Current W2 expanded-discovery manifest:
  `configs/m6d_w2_expanded_discovery_complex_targets.json`
- Current W2 expanded-discovery target-MSA plan:
  `results/m6d_w2_expanded_discovery_target_msas.sh`
- Current W2 expanded-discovery target-MSA receipt:
  `results/m6d_w2_expanded_discovery_target_msa_precompute_receipt.jsonl`
- Current W2 expanded-discovery sync-back/replay plan:
  `results/m6d_w2_expanded_discovery_sync_back.sh`
- Current W2 expanded-discovery strict pre-MSA report:
  `results/m6d_w2_expanded_discovery_targets_manifest_strict_pre_msa.json`
- Current W2 fresh-discovery pool: `results/m6d_w2_fresh_discovery_pool.{json,md}`
- Current W2 fresh-discovery manifest: `configs/m6d_w2_fresh_discovery_complex_targets.json`
- Current W2 fresh-discovery target-MSA plan: `results/m6d_w2_fresh_discovery_target_msas.sh`
- Current W2 fresh-discovery target-MSA receipt:
  `results/m6d_w2_fresh_discovery_target_msa_precompute_receipt.jsonl`
- Current W2 fresh-discovery require-files report:
  `results/m6d_w2_fresh_discovery_targets_manifest.json`
- Current W2 unique-source pilot manifest:
  `configs/m6d_w2_fresh_discovery_unique_source_pilot_targets.json`
- Current W2 unique-source pilot submit plan:
  `results/m6d_w2_fresh_discovery_unique_source_pilot_submit.sh`
- Current W2 unique-source pilot submit receipt:
  `results/m6d_w2_fresh_discovery_unique_source_pilot_submit_receipt.json`
- Current W2 unique-source pilot completion:
  `results/m6d_w2_fresh_discovery_unique_source_pilot_completion.json`
- Current W2 unique-source pilot panel report:
  `results/m6d_w2_fresh_discovery_unique_source_pilot_panel_report.json`
- Current W2 unique-source pilot diagnostic:
  `results/m6d_w2_fresh_discovery_unique_source_pilot_diagnostic.{json,md}`
- Current W2 unique-source pilot status snapshot:
  `results/m6d_project_status_w2_fresh_discovery_pilot.json`
  (W2 readout snapshot only; canonical W1/W3/W4 boundaries remain in
  `results/m6c_project_status_w2_followup.json` and this anchor.)
- W2 panel report: `results/m6d_followup_panel_report.json`
- W2 panel diagnostic: `results/m6d_followup_panel_diagnostic.json`
- W3 Chai aggregate: `results/m6c_w3_chai1_batch_aggregate_report.json`
- W3 cross-predictor report: `results/m6c_cross_predictor.json`
- W4 round summary: `results/m6c_w4_round/summary.json`

## W2 Decision Path

Current result: the v4 five-target target-family panel is evaluable but not certified at `alpha=0.2`, and
the v5 13-target target-family panel is also evaluable but not certified at `alpha=0.2`.
The `3PC8_AB` target-specific mini-scale is certified at `alpha=0.2`, but it must not be promoted
to multi-target generalization.

Chosen current W2 branch: `w2_target_family_redesign_v6_no_spend_gate_strategy`.

Current W2 branch verdict: gate-strategy resolution or expanded discovery beyond excluded sources is required
before the next Cayuga W2 submission. `configs/m6d_w2_target_family_redesign_v6_candidate_rules.json` and
`results/m6d_w2_target_family_redesign_v6_candidate_pool.json` explicitly keep
`ready_for_cayuga_submission=false`.

Historical known-pool screen verdict: `results/m6d_w2_candidate_pool_screen.json` screened all known
local W2 candidate targets across the candidate, redesign, follow-up, and fresh unique-source pilot
manifests. It has `n_candidates=12`, `n_admitted_for_pilot=0`, `ready_for_revised_manifest=false`, and
`ready_for_cayuga_submission=false`.

Current fresh-discovery verdict: `results/m6d_w2_fresh_discovery_pool.json` screened 10 public
RCSB seed structures, scanned 47 chain pairs, admitted 6 structural chain-pair candidates, and selected
6 for the pre-MSA manifest. These 6 candidates come from 3 unique source PDBs
(`1A2K`, `1AK4`, `1FQJ`), so source redundancy must be reviewed before treating them as a
multi-target generalization panel. The branch is ready for target-MSA precompute only:
`ready_for_target_msa_precompute=true`, `ready_for_cayuga_submission=false`.

Current fresh-discovery manifest check: target-MSA precompute completed on Cayuga for all 6 selected
chain-pair candidates. The submitted jobs were `3056468`-`3056473`, with receipt
`results/m6d_w2_fresh_discovery_target_msa_precompute_receipt.jsonl`. After sync-back,
`results/m6d_w2_fresh_discovery_targets_manifest.json` passes `--require-files` with
`ok=true`, `n_ready_targets=6`, and no failures.

Current unique-source pilot: to avoid treating source-redundant chain pairs as independent evidence,
`configs/m6d_w2_fresh_discovery_unique_source_pilot_targets.json` keeps one candidate per source PDB:
`1FQJ_EB`, `1AK4_BC`, and `1A2K_CB`. Its strict manifest report
`results/m6d_w2_fresh_discovery_unique_source_pilot_manifest.json` has `ok=true`, `n_ready_targets=3`,
and `results/m6d_w2_fresh_discovery_unique_source_pilot_submit.sh` is syntax-checked. This was the
predeclared pilot submit candidate, not a W2 generalization certificate.

Current unique-source pilot submission/readout: the pilot was submitted on Cayuga from the
configured `CAYUGA_BIO_SFM_ROOT` checkout. ProteinMPNN jobs are `3056479` (`1FQJ_EB`), `3056481`
(`1AK4_BC`), and `3056483` (`1A2K_CB`). Dependent Boltz jobs are `3056480`, `3056482`, and
`3056484`. The receipt is
`results/m6d_w2_fresh_discovery_unique_source_pilot_submit_receipt.json`. All three records JSONLs were
synced back locally with 100 records each, and
`results/m6d_w2_fresh_discovery_unique_source_pilot_completion.json` reports `ok=true`,
`n_completed_targets=3`. The panel readout
`results/m6d_w2_fresh_discovery_unique_source_pilot_panel_report.json` is
`panel_status=multi_target_evaluable_not_certified`, `ok=false`, `n_targets=3`, `n_records=300`.
All three targets are `not_certified`; the pooled diagnostic has `trusted=0`, `tau=null`, and
`trust_all_false_accept_rate=0.99`. This is negative W2 evidence, not a failed sync or missing-compute
state. `results/m6d_w2_fresh_discovery_unique_source_pilot_diagnostic.json` classifies all three fresh
pilot targets as `target_protocol_mismatch_low_success`; each has zero accepts under the transferred
t0.3 low-pAE cutoff, so the revised branch now rejects all three for the current W2 protocol.
`results/m6d_w2_next_branch_design.json` selects
`protocol_redesign_plus_success_enriched_discovery_v1`, keeps `ready_for_cayuga_submission=false`, and
emits `configs/m6d_w2_next_branch_candidate_rules.json` as the no-spend unlock rule set for a future
candidate pool. That rule config still has `spend_gate.cayuga_submission_allowed=false`.
`results/m6d_w2_next_branch_candidate_pool.json` now applies those rules to 25 local inventory candidates:
the previous 15 plus the expanded-discovery manifest. After the expanded target MSAs were synced back,
it has `n_admitted_for_next_branch=10`, `n_source_redundancy_audit_only=3`,
`n_target_msa_precompute_blocked=0`, and `ready_for_revised_manifest=true`. The admitted targets are
`1BVK_ED`, `1CHO_GI`, `1EER_BA`, `1HE1_CA`, `1KAC_AB`, `1KLU_BA`, `1MLC_BE`, `1NMB_HL`, `1QFW_BA`,
and `2MTA_CA`. The audit-only targets remain `1A2K_DA`, `1A2K_EB`, and `1AK4_AD`; they are
source-redundant with failed current-protocol source PDBs and must not be promoted to independent W2
evidence without a separate predeclared audit plan.
That separate plan now exists as `results/m6d_w2_source_redundancy_audit_plan.{json,md}`. It is still
`ready_for_cayuga_submission=false` and `ready_for_w2_generalization_claim=false`; it only frames a
within-source failure-mode audit.

Current expanded-discovery branch: `configs/m6d_w2_expanded_discovery_seed_rcsb_ids.json` expands beyond
the excluded current-protocol source PDBs. `results/m6d_w2_expanded_discovery_pool.json` screened 49 seed
PDBs, scanned 248 chain pairs, admitted 17 structural candidates, and selected 10 source-diverse candidates
from 10 unique source PDBs: `1BVK`, `1CHO`, `1EER`, `1HE1`, `1KAC`, `1KLU`, `1MLC`, `1NMB`, `1QFW`, and
`2MTA`. The schema-only manifest check is `ok=true` for 10 targets and emitted
`results/m6d_w2_expanded_discovery_target_msas.sh`. The strict pre-MSA report is deliberately fail-closed:
`ok=false`, `n_ready_targets=0`, `failures_by_kind={"missing_file": 20}` because target `.a3m` files and
reports have not been precomputed yet. This authorizes only target-MSA precompute on Cayuga; it does not
authorize expanded ProteinMPNN/Boltz submission or W2 generalization.

Current expanded target-MSA precompute: the first Cayuga batch (`3056521`-`3056530`) produced three
ready targets and seven ColabFold MSA-server invalid tar/gzip failures. The low-concurrency sequential
retry (`3056535`-`3056541`) completed the remaining targets. The combined audit
`results/m6d_w2_expanded_discovery_target_msa_combined_audit.{json,md}` is now
`target_msa_precompute_complete_after_retry`, with 10/10 ready targets and no in-progress or failed
targets. `results/m6d_w2_expanded_discovery_sync_back.sh` pulled the target `.a3m`/report files,
`results/m6d_w2_expanded_discovery_input_prep_completion_post_sync.json` reports `ok=true` with
70/70 non-empty input-prep artifacts, and
`results/m6d_w2_expanded_discovery_targets_manifest.json` passes strict `--require-files` with
`ok=true`, `n_ready_targets=10`, and no failures.

Current expanded next-branch manifest: `results/m6d_w2_next_branch_manifest_design.json` freezes the
10 admitted, source-diverse targets into `configs/m6d_w2_expanded_next_branch_targets.json`.
`results/m6d_w2_expanded_next_branch_manifest.json` passes strict preflight with `ok=true`,
`n_ready_targets=10`, and no failures. The submit plan is
`results/m6d_w2_expanded_next_branch_submit_plan.sh`, but this is still not W2 evidence and should not be
run bare. `results/m6d_w2_expanded_next_branch_submit_with_receipt.sh` has now submitted the expanded
panel on Cayuga with validated receipt/summary artifacts:
`results/m6d_w2_expanded_next_branch_submit_receipt.jsonl` and
`results/m6d_w2_expanded_next_branch_submit_receipt_summary.json`. ProteinMPNN jobs are `3056559`,
`3056561`, `3056563`, `3056565`, `3056567`, `3056569`, `3056571`, `3056573`, `3056575`, and
`3056577`; dependent Boltz jobs are `3056560`, `3056562`, `3056564`, `3056566`, `3056568`,
`3056570`, `3056572`, `3056574`, `3056576`, and `3056578`. Boltz job `3056576` (`1QFW_BA`) failed
fail-closed because the ProteinMPNN candidate `target_seq` included a terminal atom-only residue not
present in the CA-modeled target FASTA/MSA query; repaired candidates are in
`hpc_outputs/m6d_w2_fresh_discovery_records/1QFW_BA/candidates_proteinmpnn_complex.ca_target_seq_repaired.jsonl`,
and repair Boltz job `3056582` completed with
`results/m6d_w2_expanded_next_branch_repair_summary.json`. All 10 target records were synced back, and
`results/m6d_w2_expanded_next_branch_completion.json` is `ready_for_panel_report`
with 10/10 completed targets. The target-wise report
`results/m6d_w2_expanded_next_branch_panel_report.json` is
`multi_target_evaluable_not_certified` at alpha=0.2 with 10 targets and 1000 records. This is completed
negative W2 evidence: a pooled diagnostic can certify tau=0.4, but all 10 target-wise certificates are
`not_certified`, so W2 generalization remains unsupported. The follow-up diagnostic is
`results/m6d_w2_expanded_next_branch_redesign_diagnostic.{json,md}` and the next-actions anchor is
`results/m6d_w2_expanded_next_branch_next_actions.md`. The materialized target-family branch is
`results/m6d_w2_target_family_redesign_v1_design.{json,md}` with rules in
`configs/m6d_w2_target_family_redesign_v1_candidate_rules.json`.

Next W2 work should treat the unique-source pilot as completed negative evidence, not as a pending submit:

1. Freeze `3PC8_AB` as target-specific positive evidence.
2. Keep `1BRS_AD` as an anchor/reference, not an immediate scale target.
3. Drop or hold weak prior-panel targets that failed as target/protocol mismatches or produced zero low-pAE
   cutoff accepts under the current protocol.
4. Treat `1FQJ_EB`, `1AK4_BC`, and `1A2K_CB` as rejected for the current W2 branch unless the
   generation/evaluation protocol changes.
5. Treat the expanded next-branch panel as completed negative evidence: 10 targets, 1000 records,
   `multi_target_evaluable_not_certified`, and all 10 target-wise certificates `not_certified`.
6. Do not use the pooled diagnostic as W2 evidence; target-wise certification is the gate for W2.
7. Historical `w2_target_family_redesign_v1` step: drop or redesign `1CHO_GI`, `1EER_BA`, `1HE1_CA`,
   `1KAC_AB`, `1MLC_BE`, and `2MTA_CA`; retain/retest `1BVK_ED` and `1NMB_HL`; hold `1KLU_BA` and
   `1QFW_BA` until the low-pAE acceptance strategy is clarified.
8. The no-spend replay under `w2_target_family_redesign_v1` admits 0/25 local inventory candidates,
   blocks manifest design with 0 selected targets, and admits 0 structural candidates from the filtered
   39-seed local source cache. Do not run target-MSA precompute or Cayuga W2 jobs from this state.
9. RCSB seed expansion selected 80 new seeds from a 40,318-entry search response; structural discovery admitted
   132 chain pairs and selected 10 source-diverse targets. Sequence-diversity audit blocks the full 10-target
   panel because 8/10 targets are sequence-identical near duplicates. Do not treat source-PDB diversity as
   target-family diversity.
10. The 3 representative RCSB targets `10XZ_EF`, `10YB_GH`, and `12NP_AH` completed target-MSA prep after
    a transient 10XZ retry. The frozen representative manifest passed strict preflight and completed a scoped
    ProteinMPNN/Boltz probe with receipt capture: ProteinMPNN jobs `3056651`, `3056653`, `3056655`; Boltz
    jobs `3056652`, `3056654`, `3056656`. Completion is `ok=true` with 3/3 targets and 300 records, but
    `results/m6d_w2_target_family_redesign_v1_rcsb_representative_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2. All three target-wise certificates are
    `not_certified`, so this remains a representative designability readout, not broad W2 evidence.
11. The representative diagnostic
    `results/m6d_w2_target_family_redesign_v1_rcsb_representative_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`: replace or redesign `12NP_AH` before more W2 panel GPU, and do
    not scale `10XZ_EF` or `10YB_GH` until a low-pAE acceptance or target-specific calibration strategy is
    predeclared.
12. `w2_target_family_redesign_v2` carries those representative-panel lessons forward. It excludes
    `12NP_AH`, holds `10XZ_EF`/`10YB_GH` under the current signal strategy, and rescreens the current local
    inventory with no GPU spend. `results/m6d_w2_target_family_redesign_v2_candidate_pool.json` admits 0/25
    candidates, keeps `1A2K_DA`, `1A2K_EB`, and `1AK4_AD` source-redundancy audit-only, and is not ready for
    a revised manifest or Cayuga submission.
13. The current `w2_target_family_redesign_v2_rcsb` branch expanded beyond excluded v2 sources without live
    API spend by replaying the saved RCSB response. It selected 80 new seed PDBs from 500 response IDs
    (`response_total_count=40318`), screened 423 chain pairs, admitted 38 structural candidates, and selected
    20 source-unique targets from 20 source PDBs.
14. Sequence-diversity audit now passes the predeclared breadth gate for the 20-target v2 RCSB manifest:
    20 targets, 12 sequence clusters, largest cluster size 5, largest-cluster fraction 0.25, with
    `min_clusters=4` and `max_largest_cluster_fraction=0.4`.
15. Target-MSA precompute completed on Cayuga after low-concurrency retry. The first batch submitted 20 jobs
    (`3056683`-`3056702`) and reached 9/20 ready; the retry wrapper submitted 11 sequential jobs
    (`3056703`-`3056713`) and recovered all remaining targets. Post-sync strict preflight now passes:
    `ok=true`, `n_ready_targets=20`, no failures.
16. The v2 RCSB panel was submitted on Cayuga through the receipt-preserving wrapper. The receipt
    records 20 ProteinMPNN/Boltz job pairs: ProteinMPNN jobs `3056715`-`3056753` and Boltz jobs
    `3056716`-`3056754`. Records are isolated under
    `hpc_outputs/m6d_w2_target_family_redesign_v2_rcsb_records`.
17. The v2 RCSB records are now synced back and completed. The target-wise panel report
    `results/m6d_w2_target_family_redesign_v2_rcsb_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2 with 20 targets and 2000 records. Only `1A3O_BA`
    and `1A9W_EA` certify target-wise; the other 18 targets do not. The pooled diagnostic certifies
    tau=1.0, but pooled-only evidence is not W2 generalization.
18. The v2 RCSB redesign diagnostic
    `results/m6d_w2_target_family_redesign_v2_rcsb_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`, with `1A1R_BA` as the explicit low-success drop/redesign
    target and several high-success/non-certified targets held for signal or split-sensitivity follow-up.
19. The active follow-on branch is `w2_target_family_redesign_v3`. It preserves `1A3O_BA` and `1A9W_EA` as
    target-specific positive controls, not W2 generalization evidence. Live RCSB seed expansion selected 80
    new seeds from 1000 response IDs; local structural intake screened 753 chain pairs, admitted 31
    structural candidates, and selected 8 source-diverse targets: `1AOI_HB`, `1AR1_CD`, `1AP2_BA`,
    `1AFQ_BC`, `1AVO_DC`, `1AJ9_BA`, `1APY_CD`, and `1AQD_ED`.
20. The v3 sequence-diversity audit passes with 8 targets, 8 sequence clusters, and largest-cluster
    fraction 0.125. Pre-MSA manifest validation passes 8/8, and Cayuga target-MSA precompute jobs
    `3057081`-`3057088` were submitted with receipt
    `results/m6d_w2_target_family_redesign_v3_target_msa_precompute_receipt.jsonl`. Six targets are ready
    remotely (`1AR1_CD`, `1AP2_BA`, `1AFQ_BC`, `1AJ9_BA`, `1APY_CD`, `1AQD_ED`). `1AOI_HB` and `1AVO_DC`
    failed with the known ColabFold MSA-server invalid tar/gzip response mode and were resubmitted as
    low-concurrency retry jobs `3057090` and `3057091` with receipt
    `results/m6d_w2_target_family_redesign_v3_target_msa_retry_receipt.jsonl`. Retry recovered 8/8 target
    MSAs, sync-back completed, and strict post-MSA preflight passes 8/8.
21. The v3 panel was submitted on Cayuga through
    `results/m6d_w2_target_family_redesign_v3_submit_with_receipt.sh`. ProteinMPNN/Boltz job pairs are
    `3057094`-`3057109`, and records are isolated under
    `hpc_outputs/m6d_w2_target_family_redesign_v3_records`. Records are now synced back 8/8, and
    `results/m6d_w2_target_family_redesign_v3_completion.json` is `ready_for_panel_report`.
22. The v3 target-wise panel report
    `results/m6d_w2_target_family_redesign_v3_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2 with 8 targets and 800 records. All eight
    target-wise certificates are `not_certified`. The pooled diagnostic certifies tau=0.75 with 237 trusted
    test records, but pooled-only evidence is not W2 generalization.
23. The v3 redesign diagnostic
    `results/m6d_w2_target_family_redesign_v3_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`: replace or redesign `1AOI_HB` and `1APY_CD` before more panel
    GPU; treat `1AFQ_BC`, `1AJ9_BA`, `1AP2_BA`, `1AQD_ED`, `1AR1_CD`, and `1AVO_DC` only as split/cutoff
    sensitivity anchors unless a revised protocol is predeclared.
24. The follow-on v4 branch is completed negative/evaluable, not W2 evidence. It froze stricter rules, screened
    56 local inventory candidates with 0 admitted and 3 source-redundancy audit-only, selected 80 new RCSB
    seeds from a 1500-row live search, scanned 527 chain pairs, selected 7 source-diverse structural
    candidates, passed sequence-diversity audit with 7 targets, 5 clusters, and largest-cluster fraction
    0.428571, then selected a 5-target representative manifest: `1AY7_AB`, `1B27_CF`, `1B2S_BE`,
    `1AZZ_DC`, and `1AYY_BA`.
25. V4 target-MSA precompute completed 5/5 after the known ColabFold MSA-server tar/gzip failure hit
    `1AY7_AB` and `1B2S_BE`; low-concurrency retry jobs `3057130` and `3057131` recovered both targets.
    Strict post-MSA preflight
    `results/m6d_w2_target_family_redesign_v4_manifest_post_msa_require_files.json` passes 5/5.
26. The v4 representative panel was submitted on Cayuga through
    `results/m6d_w2_target_family_redesign_v4_submit_with_receipt.sh`. ProteinMPNN/Boltz job pairs are
    `3057134`-`3057143`, with records isolated under
    `hpc_outputs/m6d_w2_target_family_redesign_v4_records`. The v4 records synced back and
    `results/m6d_w2_target_family_redesign_v4_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2 with 5 targets and 500 records. `1AY7_AB` and
    `1B27_CF` are target-specific certified controls; the other three targets are not certified.
27. The follow-on v5 branch is also completed negative/evaluable, not W2 evidence. It admitted 13
    source-diverse next-branch targets, passed strict pre-submit preflight 13/13, and completed
    receipt-validated ProteinMPNN/Boltz job pairs `3057168`-`3057193` through
    `results/m6d_w2_target_family_redesign_v5_submit_with_receipt.sh`.
28. The v5 synced panel report `results/m6d_w2_target_family_redesign_v5_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2 with 13 targets and 1300 records. All 13
    target-wise certificates are `not_certified`. The diagnostic
    `results/m6d_w2_target_family_redesign_v5_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`; do not submit another W2 panel without a predeclared
    low-pAE/target-specific calibration or generation-protocol redesign.
29. The v5 gate-strategy replay `results/m6d_w2_target_family_redesign_v5_gate_strategy.{json,md}` is a
    no-spend decision artifact, not W2 evidence. It separates label-degenerate all-success targets
    (`1BFV_HL`, `1BQH_KI`), low-pAE acceptance holds (`1B2U_CF`, `1B86_DC`, `1BCP_KL`), target/protocol
    replacement targets (`1BE3_EJ`, `1BGY_EJ`, `1BHH_AB`, `1BMQ_AB`, `1BQL_LY`, `1BQQ_TM`),
    split-sensitive `1BGS_BF`, and target-specific calibration target `1B3S_CF`.
30. The v6 no-spend branch links that gate strategy into
    `configs/m6d_w2_target_family_redesign_v6_candidate_rules.json`. Current inventory screening in
    `results/m6d_w2_target_family_redesign_v6_candidate_pool.{json,md}` admits 0/25 targets and marks only
    `1A2K_DA`, `1A2K_EB`, and `1AK4_AD` as source-redundancy audit-only. This does not authorize a revised
    manifest, target-MSA precompute, or Cayuga ProteinMPNN/Boltz submission.
31. The v7 protocol `configs/m6d_w2_target_family_redesign_v7_protocol.json`, generated from
    `results/m6d_w2_target_family_redesign_v6_gate_strategy_resolution.{json,md}`, resolves the gate
    blockers fail-closed. All-success label-degenerate targets remain positive controls only; fixed low-pAE
    cutoff transfer is forbidden as a certificate; target-specific or explicitly family-calibrated RCPS must
    be predeclared before records are generated; and any W2 panel claim requires at least three non-anchor
    source-diverse targets with target-wise alpha<=0.2 certificates. At protocol-resolution time this was a
    no-manifest/no-submit state; the later v7 representative branch below is now completed negative/evaluable,
    not a W2 generalization unlock.
32. The v7 discovery pass found a candidate branch without Cayuga/GPU spend. Seed expansion
    `results/m6d_w2_target_family_redesign_v7_seed_expansion.{json,md}` considered 5000 response IDs and
    selected 80 new RCSB seeds. Local structural intake
    `results/m6d_w2_target_family_redesign_v7_discovery_pool.{json,md}` fetched those seeds, screened 566
    chain pairs, admitted 30 structurally, and selected 13 source-diverse candidates.
33. The full 13-target v7 manifest is not authorized for full-panel target-MSA or W2 claims because
    `results/m6d_w2_target_family_redesign_v7_sequence_diversity.json` is
    `sequence_diversity_dominated_by_near_duplicates`: 13 targets, 10 clusters, largest-cluster fraction
    0.307692. The representative subset `configs/m6d_w2_target_family_redesign_v7_representative_targets.json`
    has 10 targets, 10 clusters, largest-cluster fraction 0.1, and
    `results/m6d_w2_target_family_redesign_v7_representative_manifest_pre_msa.json` passes 10/10 pre-MSA.
    The only next Cayuga step opened by this state is target-MSA input prep through
    `results/m6d_w2_target_family_redesign_v7_target_msas.sh`, first with
    `TARGET_MSA_PRECOMPUTE_DRY_RUN=1`. Do not emit or run a v7 ProteinMPNN/Boltz submit plan before
    target-MSA files sync back and strict `--require-files` passes.
34. The v7 representative target-MSA dry-run passed locally and on Cayuga, then target-MSA precompute was
    submitted on Cayuga for the 10 representative targets only. Receipt
    `results/m6d_w2_target_family_redesign_v7_target_msa_precompute_receipt.jsonl` records jobs
    `3057255`-`3057264` for `1C08_BA`, `1BVL_CD`, `1BZQ_BN`, `1BXI_BA`, `1BZ0_DC`, `1C40_BA`,
    `1C1Y_AB`, `1C5F_CE`, `1BUV_TM`, and `1BX2_ED`. At that checkpoint the state was waiting on MSA
    outputs. This is input-prep provenance only, not W2 evidence and not v7 ProteinMPNN/Boltz submission
    authority.
    After completion, use `results/m6d_w2_target_family_redesign_v7_msa_sync_back.sh` to pull MSA outputs
    and rerun strict post-MSA manifest preflight.
35. V7 target-MSA precompute completed 10/10 after the known ColabFold/MMseqs truncated-tar failure mode hit
    `1C08_BA` and `1C1Y_AB`; low-concurrency retry jobs `3057265` and `3057266` completed successfully.
    `results/m6d_w2_target_family_redesign_v7_representative_manifest_post_msa_require_files.json` is
    `ok=true` with 10/10 ready targets.
36. The v7 representative panel completed on Cayuga through
    `results/m6d_w2_target_family_redesign_v7_submit_with_receipt.sh`. ProteinMPNN/Boltz job pairs were
    `3057268`/`3057269`, `3057270`/`3057271`, `3057272`/`3057273`, `3057274`/`3057275`,
    `3057276`/`3057277`, `3057278`/`3057279`, `3057280`/`3057281`, `3057282`/`3057283`,
    `3057284`/`3057285`, and `3057286`/`3057287`. Receipt validation passed 10/10 in
    `results/m6d_w2_target_family_redesign_v7_submit_receipt.jsonl`; records are synced back and
    `results/m6d_w2_target_family_redesign_v7_completion.json` is `ready_for_panel_report` with 10/10
    completed targets.
37. The v7 target-wise report
    `results/m6d_w2_target_family_redesign_v7_panel_report.json` is
    `multi_target_evaluable_not_certified` at alpha=0.2 with 10 targets, 1000 records, and 0/10
    target-wise certificates. The redesign diagnostic
    `results/m6d_w2_target_family_redesign_v7_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`: `1BUV_TM`, `1BZQ_BN`, and `1C5F_CE` are low-success
    target/protocol mismatch candidates; `1BVL_CD`, `1BX2_ED`, `1BXI_BA`, `1BZ0_DC`, `1C08_BA`,
    `1C1Y_AB`, and `1C40_BA` are low-pAE-acceptance holds. V7 is a completed negative/evaluable science
    result, not W2 generalization evidence. Do not spend more W2 panel GPU until target replacement or
    generation/gate redesign is predeclared.
38. The next no-spend branch is now predeclared as
    `w2_target_family_redesign_v8_success_enriched_gate_redesign`.
    `results/m6d_w2_target_family_redesign_v8_followup_contract.{json,md}` and
    `configs/m6d_w2_target_family_redesign_v8_candidate_rules.json` freeze the follow-up contract:
    Cayuga submission is `false`, all 10 v7 target/source IDs are excluded under the current protocol,
    fixed low-pAE cutoff transfer is still forbidden, and unlock requires at least three non-anchor
    source-diverse targets, sequence-diversity/preflight, and a predeclared low-pAE or target-specific
    calibration rule.
39. The v8 no-spend screen has now produced an input-prep-ready branch, still not W2 evidence. RCSB seed
    expansion `results/m6d_w2_target_family_redesign_v8_seed_expansion.{json,md}` selected 100 seeds from
    8000 response IDs after prior/current exclusions. Local structural intake
    `results/m6d_w2_target_family_redesign_v8_discovery_pool.{json,md}` screened 641 chain pairs, admitted
    29 structurally, and selected 12 source-diverse candidates. Sequence diversity
    `results/m6d_w2_target_family_redesign_v8_sequence_diversity.{json,md}` passes with 12 targets,
    8 clusters, and largest-cluster fraction 0.25. Pre-MSA manifest validation
    `results/m6d_w2_target_family_redesign_v8_manifest_pre_msa.json` is `ok=true` with 12/12 ready targets.
40. The v8 target-MSA bridge completed input prep but is still not W2 evidence. Initial Cayuga target-MSA
    jobs `3057296`-`3057307` produced 7/12 stable MSAs and hit the known transient ColabFold/MMseqs
    truncated-tar failure mode on `1CL7_HI`, `1CG8_AB`, `1CG5_AB`, `1CMY_DC`, and `1CPB_BA`. The serial
    retry bridge `results/m6d_w2_target_family_redesign_v8_target_msa_retry_serial.sh` submitted retry jobs
    `3057334`-`3057338`, all completed `0:0`, and recovered the remaining five target MSAs. Initial and
    retry provenance are in
    `results/m6d_w2_target_family_redesign_v8_target_msa_precompute_receipt.jsonl`,
    `results/m6d_w2_target_family_redesign_v8_target_msa_precompute_receipt_summary.json`,
    `results/m6d_w2_target_family_redesign_v8_target_msa_retry_receipt.jsonl`, and
    `results/m6d_w2_target_family_redesign_v8_target_msa_retry_receipt_summary.json`.
41. V8 post-sync input-prep gates now pass. `results/m6d_w2_target_family_redesign_v8_msa_sync_back.sh`
    synced all target-MSA/report paths back locally;
    `results/m6d_w2_target_family_redesign_v8_input_prep_completion_post_sync.json` is `ok=true` with
    84/84 nonempty artifacts and zero pending paths; and
    `results/m6d_w2_target_family_redesign_v8_manifest_post_msa_require_files.json` passes strict
    post-MSA `--require-files` with 12/12 ready targets and zero failures. This opens only the next planning
    gate: generate a separate receipt-preserving ProteinMPNN/Boltz panel plan, completion check, and
    panel-report path before any v8 panel submission. Do not claim W2 generalization from input-prep alone.
42. The v8 panel plan gate is now open and submitted. `results/m6d_w2_target_family_redesign_v8_submit_ready.json`
    passes 12/12, `results/m6d_w2_target_family_redesign_v8_submit_plan.sh` is the bare generated plan,
    `results/m6d_w2_target_family_redesign_v8_submit_with_receipt.sh` is the only approved submit entrypoint,
    and `results/m6d_w2_target_family_redesign_v8_completion.sh` plus
    `results/m6d_w2_target_family_redesign_v8_sync_back.sh` define the post-job replay path. Local and Cayuga
    dry-runs passed. The receipt wrapper submitted 12 ProteinMPNN/Boltz job pairs on Cayuga:
    `3057340`-`3057363`, summarized in
    `results/m6d_w2_target_family_redesign_v8_submit_receipt_summary.json`. This is job provenance only;
    W2 evidence requires synced records, `results/m6d_w2_target_family_redesign_v8_completion.json`,
    `results/m6d_w2_target_family_redesign_v8_panel_report.json`, and target-wise certification.
43. The v8 panel is now synced back and evaluable but not certified for W2 generalization.
    `results/m6d_w2_target_family_redesign_v8_completion.json` is `ready_for_panel_report` with 12/12
    completed targets, and `results/m6d_w2_target_family_redesign_v8_panel_report.json` is
    `multi_target_evaluable_not_certified` at α=0.2 with 12 targets and 1200 records. Only `1CG5_AB`
    and `1COH_BA` are target-specific certified controls; the other 10 targets are not certified. The
    pooled α=0.2 certificate is diagnostic only and must not be promoted to W2 generalization.
44. The next W2 branch is a no-spend redesign contract, not a Cayuga submit plan:
    `w2_target_family_redesign_v9_positive_controls_plus_calibration_redesign`.
    `results/m6d_w2_target_family_redesign_v9_followup_contract.{json,md}` and
    `configs/m6d_w2_target_family_redesign_v9_candidate_rules.json` set
    `ready_for_cayuga_submission=false`. Retain `1CG5_AB` and `1COH_BA` only as positive controls;
    exclude the 10 non-certified v8 targets under the current protocol; run replacement-target discovery
    or gate-redesign screening before any more W2 ProteinMPNN/Boltz GPU spend.
45. The v9 no-spend replacement-target screen now passes local gates. RCSB seed expansion selected
    120 new seeds from 10,000 response IDs
    (`results/m6d_w2_target_family_redesign_v9_seed_expansion.{json,md}`), local structural intake
    screened 988 chain pairs and selected 14 source-diverse targets
    (`results/m6d_w2_target_family_redesign_v9_discovery_pool.{json,md}`), and sequence diversity passes
    with 14 clusters
    (`results/m6d_w2_target_family_redesign_v9_sequence_diversity.{json,md}`). The representative manifest
    is `configs/m6d_w2_target_family_redesign_v9_representative_targets.json`;
    `results/m6d_w2_target_family_redesign_v9_manifest_pre_msa.json` is `ok=true` with 14/14 ready targets;
    `results/m6d_w2_target_family_redesign_v9_target_msas.sh` and
    `results/m6d_w2_target_family_redesign_v9_target_msa_with_receipt.sh` dry-run cleanly.
    Strict post-MSA `--require-files` currently fails closed on 28 missing target-MSA/MSA-report files in
    `results/m6d_w2_target_family_redesign_v9_manifest_post_msa_require_files.json`. The next Cayuga action
    is target-MSA input prep only; do not emit or run a v9 ProteinMPNN/Boltz panel submit plan before MSA
    outputs sync back and strict `--require-files` passes.
46. V9 target-MSA presubmit preflight is ready but not submitted.
    `results/m6d_w2_target_family_redesign_v9_target_msa_presubmit_preflight.{json,md}` records remote
    readback: Cayuga runtime/helper files are present, wrapper dry-run passes, 14/14 representative targets
    have source/prepared/FASTA inputs synced, and the remaining blocker is exactly the 28 missing
    target-MSA/MSA-report files. Real target-MSA submission requires explicit approval and must use
    `results/m6d_w2_target_family_redesign_v9_target_msa_with_receipt.sh`; the wrapper refuses to touch the
    receipt unless `BIO_SFM_APPROVE_V9_TARGET_MSA=approve-v9-target-msa-precompute` is present. W2 panel
    submission remains blocked.
47. V9 postsubmit replay is prepared but not executed. The current pre-sync completion report
    `results/m6d_w2_target_family_redesign_v9_input_prep_completion_pre_sync.json` is fail-closed with
    70/98 artifacts present and 28 missing target-MSA/MSA-report files. Those paths are listed in
    `results/m6d_w2_target_family_redesign_v9_pending_input_prep_paths.txt`. After approved target-MSA jobs
    finish, run `results/m6d_w2_target_family_redesign_v9_msa_sync_back.sh`; it pulls the 28 outputs plus
    `results/m6d_w2_target_family_redesign_v9_target_msa_precompute_receipt.jsonl` and summary, then reruns
    input-prep completion and strict `--require-files`. The replay boundary is recorded in
    `results/m6d_w2_target_family_redesign_v9_postsubmit_replay_plan.{json,md}`, and that plan records the
    same approval-env-gated pre-submit command as the presubmit preflight.
48. V9 target-MSA gate audit passes without submitting work:
    `results/m6d_w2_target_family_redesign_v9_target_msa_gate_audit.{json,md}` reports
    `audit_ok=true`, 14 targets, 28 pending target-MSA/MSA-report paths, 70/98 input-prep artifacts
    present, and `ready_for_panel_submission=false`. This audit means the expected-blocked state is
    coherent, including agreement between the presubmit and postsubmit approval-env-gated submit commands;
    it is not W2 evidence and it does not remove the explicit-approval requirement.
49. Project status now consumes the v9 gate audit:
    `results/m6c_project_status_w2_followup.json` reports W2 as
    `panel_approval_packet_ready_awaiting_explicit_approval`, with the older evaluable-not-certified panel
    report and target-MSA gate audit retained as superseded/staged evidence. This keeps the machine resume
    point aligned with the v9 branch without treating panel approval readiness as W2 evidence.
50. V9 target-MSA approval packet is ready but does not submit:
    `results/m6d_w2_target_family_redesign_v9_approval_packet.{json,md}` reports
    `approval_packet_ready=true`, 14 targets, and 28 pending target-MSA/MSA-report paths. It verifies that
    project status, the v9 gate audit, the representative manifest, pending-path list, submit wrapper, and
    sync-back replay script agree. It authorizes no ProteinMPNN/Boltz panel work; it only records the exact
    target-MSA input-prep command to use if explicit approval is later given. The main project status now
    consumes this packet through `--w2-approval-packet`.
    `results/m6d_w2_explicit_approval_runbook.{json,md}` is the matching operator-facing no-submit runbook:
    it records the target-MSA-only command order, requires explicit user approval first, and keeps panel
    work blocked until sync-back plus strict replay pass.
51. V9 target-MSA wrapper guard audit passes without submitting:
    `results/m6d_w2_target_family_redesign_v9_wrapper_guard_audit.{json,md}` reports static guard success
    and a no-env wrapper execution that exits before receipt initialization. The v9 target-MSA receipt stays
    absent locally and on Cayuga, so this proves the approval boundary without authorizing target-MSA or panel
    work.
52. V9 local/Cayuga approval-packet parity passes:
    `results/m6d_w2_target_family_redesign_v9_approval_parity.{json,md}` reports `parity_ok=true` for the
    local and Cayuga approval packets, with 14 targets, 28 pending target-MSA/MSA-report paths,
    `approval_packet_ready=true`, and panel submission blocked on both sides. This is a no-submit check
    and does not create a target-MSA receipt. The main project status now consumes this parity report
    through `--w2-approval-parity`.
53. Project-level post-sync replay now has dependency provenance:
    `results/m6c_project_w2_followup_post_sync.manifest.json` records the union of current input-prep and
    external pending paths, and `results/m6c_project_w2_followup_post_sync.sh` checks that manifest before
    local replay. The refreshed W2 project status reports `script_manifests=4/4` and
    `sync_manifest_audit=ok checks=4` while W2 remains incomplete behind explicit panel approval and
    target-wise certification.
54. V9 target-MSA wrapper is now fail-closed on accidental real execution:
    the wrapper guard audit confirms that running
    `results/m6d_w2_target_family_redesign_v9_target_msa_with_receipt.sh` without
    `BIO_SFM_APPROVE_V9_TARGET_MSA=approve-v9-target-msa-precompute` exits before creating or truncating
    `results/m6d_w2_target_family_redesign_v9_target_msa_precompute_receipt.jsonl`. Dry-run remains available
    without the approval env and still does not touch the receipt.
55. Local goal-mode harness is stabilized:
    `results/m6d_goal_mode_local_harness_status.{json,md}` records
    `active_local_harness_stabilized`; focused contamination tests pass, and the full local WIP suite is
    `676 passed` with `/tmp/bio_sfm_science_venv/bin/python -m pytest -q`. The current repair closes the
    local generated-bridge `python` fallback issue, shared `subprocess.run` monkeypatch leakage, and
    second-predictor missing-file fixture-state leakage, stale project-status/audit wording around
    the new W2 panel approval packet, W3 runtime-probe provenance after the target Cayuga
    no-submit probe, W3 runtime repair-plan consumption, and W3 guarded runtime provision-packet
    consumption. The current W3 addition records the probe as runtime-blocked and emits a guarded runtime
    validation packet, not execution inputs.
56. The approved W2 v9 full-14 target-MSA input-prep attempt has synced and passed strict replay:
    `results/m6d_w2_target_family_redesign_v9_full14_target_msa_execution_attempt.{json,md}` reports
    `target_msa_outputs_synced_strict_require_files_passed`, latest checked `2026-07-05T09:08:12+09:00`.
    The first pass submitted jobs `3059563`-`3059576`; 11 completed and 3 hit the known ColabFold/MMseqs
    truncated tar/gzip response. The same audited target-MSA wrapper retried only those 3 targets as
    `3059577`-`3059579`, all completed. Sync-back passed with `98/98` input-prep artifacts, strict
    manifest `14/14` targets ready, and post-sync pending paths `0`. This is target-MSA input-prep
    provenance only, not W2 evidence and not a panel submission. The next W2 boundary is the v9
    ProteinMPNN/Boltz panel step plus target-wise certification before any generalization claim.
57. V9 ProteinMPNN/Boltz panel preflight now passes locally, but the panel has not been submitted:
    `results/m6d_w2_target_family_redesign_v9_panel_preflight.{json,md}` records
    `panel_preflight_dry_run_passed_not_submitted`. The strict submit-ready report
    `results/m6d_w2_target_family_redesign_v9_submit_ready.json` has `ok=true`, `14/14` ready targets,
    `min_contacts=20`, and no failures. The raw submit plan is
    `results/m6d_w2_target_family_redesign_v9_submit_plan.sh`; the receipt-preserving entrypoint is
    `results/m6d_w2_target_family_redesign_v9_submit_with_receipt.sh`; follow-up replay artifacts are
    `results/m6d_w2_target_family_redesign_v9_completion.sh` and
    `results/m6d_w2_target_family_redesign_v9_sync_back.sh`. Dry-run made no `sbatch` calls and left the
    v9 submit receipt/summary absent. This is still local preflight only, not W2 evidence and not a
    generalization claim.
58. V9 panel submission is now guarded by an explicit approval env and an approval packet, still with no
    panel submission:
    `results/m6d_w2_target_family_redesign_v9_panel_wrapper_guard_audit.{json,md}` reports
    `panel_wrapper_guard_ok`; non-dry wrapper execution without
    `BIO_SFM_APPROVE_V9_PANEL=approve-v9-panel-submit` exits with code 2 before creating
    `results/m6d_w2_target_family_redesign_v9_submit_receipt.jsonl`. The no-submit packet
    `results/m6d_w2_target_family_redesign_v9_panel_approval_packet.{json,md}` reports
    `panel_approval_packet_ready`, `can_submit_panel_if_user_explicitly_approves=true`, and
    `can_claim_w2_generalization=false`. This packet fixes the guarded Cayuga submit command and
    post-job sync-back command, but it is not approval, not a submitted panel, and not W2 evidence. The
    main project status now consumes this packet through `--w2-panel-approval-packet` and reports W2 as
    `panel_approval_packet_ready_awaiting_explicit_approval` with `complete=false`.
59. Goal completion/drift audits now consume the v11 panel approval, decision, and remote-readiness packets:
    `results/m6d_goal_completion_audit.{json,md}` keeps `can_mark_goal_complete=false` and records W2 as
    the sole remaining requirement. `results/m6d_goal_drift_audit.{json,md}` reports
    `no_major_direction_drift_w2_blocked`, `major_direction_drift=false`, and execution
    `panel_postsync_interpretation_predeclared_not_synced`. The next action remains explicit user approval
    before guarded W2 v11 panel submission, followed by sync-back, completion, target-wise reporting, and
    refreshed post-sync interpretation.
60. V9 post-panel decision protocol is predeclared without submitting:
    `results/m6d_w2_target_family_redesign_v9_panel_decision_protocol.{json,md}` reports
    `post_panel_decision_protocol_ready`, `no_submit=true`, and
    `can_claim_w2_generalization_now=false`. It fixes the alpha=0.2, 14-target interpretation rules for
    future synced panel records: completion/record-contract failures produce no claim; every target must
    certify target-wise for W2 generalization; partial certificates are target-specific only; pooled
    diagnostic success cannot override target-wise failure.
61. The guarded W2 v9 full-14 ProteinMPNN/Boltz panel has completed and is evaluable but not certified:
    `results/m6d_w2_target_family_redesign_v9_completion.json` is `ready_for_panel_report` with 14/14
    completed targets, and `results/m6d_w2_target_family_redesign_v9_panel_report.json` is
    `multi_target_evaluable_not_certified` at α=0.2 with 14 targets and 1400 records. The pooled diagnostic
    certifies α=0.2, but pooled-only evidence remains diagnostic; only `1DLF_HL` is target-wise certified
    (`τ=0.333`), and the other 13 targets are not certified. The refreshed
    `results/m6c_project_status_w2_followup.json` now reports W2 as `evaluable_not_certified`.
62. V9 post-panel diagnosis freezes the next branch as no-spend:
    `results/m6d_w2_target_family_redesign_v9_redesign_diagnostic.{json,md}` recommends
    `redesign_or_replace_low_success_targets`, and
    `results/m6d_w2_target_family_redesign_v9_gate_strategy.{json,md}` reports
    `no_spend_gate_strategy_required` with Cayuga submission disabled. Seven targets are low-success
    target/protocol mismatches (`1AK4_BC`, `1CHO_GI`, `1CVS_CA`, `1CXZ_AB`, `1DGR_WV`, `1DGW_YX`,
    `1DJ7_AB`); five all-success targets (`1BVK_ED`, `1D3B_LK`, `1DKE_BA`, `1DL7_HL`, `1DLH_BA`)
    require a label-degeneracy policy before any gate claim; `1D2Z_BA` is a loose-α anchor rather than an
    α=0.2 certificate; `1DLF_HL` is the single certified control.
63. V10 follow-up contract and candidate rules are now the W2 resume point:
    `results/m6d_w2_target_family_redesign_v10_followup_contract.{json,md}` records branch
    `w2_target_family_redesign_v10_post_v9_decision`, status
    `mixed_target_readout_redesign_contract_required`, and `ready_for_cayuga_submission=false`.
    `configs/m6d_w2_target_family_redesign_v10_candidate_rules.json` keeps Cayuga submission blocked until
    no-spend replacement-target discovery or a predeclared target/family calibration redesign admits at
    least three non-anchor targets and strict post-MSA/pre-submit gates pass.
64. V10 no-spend replacement-target discovery has passed the local source/sequence gates:
    `results/m6d_w2_target_family_redesign_v10_seed_expansion.{json,md}` selected 160 new RCSB seeds after
    excluding 700 previously screened seeds and the v9 blocked/control sources. Local structural intake
    `results/m6d_w2_target_family_redesign_v10_discovery_pool.{json,md}` screened 1082 chain pairs,
    admitted 42 structurally, and selected 16 source-diverse candidates. One source fetch failed
    (`11GY`, HTTP 404) and is recorded as data, not a blocker.
65. V10 sequence diversity passes before MSA spend:
    `results/m6d_w2_target_family_redesign_v10_sequence_diversity.{json,md}` reports
    `sequence_diversity_ready_for_broad_w2_panel`, 16 targets, 15 clusters, largest cluster size 2, and
    largest cluster fraction 0.125 under the v10 0.25 cap. The representative manifest
    `configs/m6d_w2_target_family_redesign_v10_representative_targets.json` keeps 15 targets after dropping
    the near-duplicate `1DXV_DC` from the `1DXU_DC`/`1DXV_DC` cluster.
66. V10 target-MSA precompute is complete after one repair pass:
    `results/m6d_w2_target_family_redesign_v10_manifest_pre_msa.json` was `ok=true` with 15/15 ready
    targets and emitted `results/m6d_w2_target_family_redesign_v10_target_msas.sh`. Cayuga submitted
    target-MSA jobs `3073806`-`3073820`; three transient ColabFold/MSA-server fetch failures
    (`1E44_BA`, `1DSF_HL`, `1EM8_AB`) were repaired with jobs `3073822`-`3073824`, recorded in
    `results/m6d_w2_target_family_redesign_v10_target_msa_repair_receipt.jsonl`. Remote-check then passed
    with all 30 MSA/report artifacts present, external sync-back pulled them locally, and
    `results/m6d_w2_target_family_redesign_v10_manifest_post_msa_require_files.json` is now `ok=true`
    with 15/15 ready targets.
67. V10 input prep and readiness are now panel-ready:
    `results/m6d_w2_target_family_redesign_v10_input_prep_completion_pre_sync.json` is
    `ready_for_require_files` with 105/105 artifacts nonempty, and
    `results/m6d_w2_target_family_redesign_v10_readiness.json` is `ready` with 15 ready targets for
    panel submission. `results/m6d_w2_target_family_redesign_v10_submit_panel.sh` is the v10-specific
    panel submit script.
68. V10 panel submission completed on Cayuga:
    `results/m6d_w2_target_family_redesign_v10_submit_panel.sh` passed remote manifest preflight and
    submitted 15 ProteinMPNN/Boltz dependency pairs, logged in
    `results/m6d_w2_target_family_redesign_v10_submit_panel.log` and summarized in
    `results/m6d_w2_target_family_redesign_v10_submit_receipt.json`. ProteinMPNN jobs are
    `3073830,3073832,3073834,3073836,3073838,3073840,3073842,3073844,3073846,3073848,3073850,3073852,3073854,3073856,3073858`;
    dependent Boltz jobs are
    `3073831,3073833,3073835,3073837,3073839,3073841,3073843,3073845,3073847,3073849,3073851,3073853,3073855,3073857,3073859`.
    All final Boltz jobs, including `3073855`, `3073857`, and `3073859`, completed with exit code `0:0`.
    The records were synced back from
    `hpc_outputs/m6d_w2_target_family_redesign_v10_records/*/records_boltz_complex.jsonl`.
69. V10 full-panel readout is completed negative evidence, not a W2 certificate:
    `results/m6d_w2_target_family_redesign_v10_panel_completion.json` is `ready_for_panel_report`
    with 15/15 completed targets, and
    `results/m6d_w2_target_family_redesign_v10_panel_report_alpha02.json` is
    `multi_target_evaluable_not_certified` at α=0.2 with 15 targets and 1500 records. The pooled
    diagnostic certifies α=0.2 (`tau=0.793`, 238 trusted test records, false-accept rate 0.067), but
    pooled-only evidence remains diagnostic. Target-wise, only `1DXU_DC` certifies at α=0.2; 14 targets
    remain not certified. The α=0.3 report
    (`results/m6d_w2_target_family_redesign_v10_panel_report_alpha03.json`) also remains
    `multi_target_evaluable_not_certified`; `1DXU_DC`, `1E44_BA`, `1EM8_AB`, and `1EMV_BA` certify
    target-wise, while 11 targets still do not. One-class all-success targets remain intentionally
    non-certified under `one_class_all_success_no_rcps_tau`; one-class all-failure targets remain
    non-certified under `one_class_all_failure_no_rcps_tau`.
70. V10 redesign diagnostics freeze the next no-spend branch:
    `results/m6d_w2_target_family_redesign_v10_redesign_diagnostic_alpha02.{json,md}` recommends
    `redesign_or_replace_low_success_targets`. It classifies `13IO_HC`, `1DS6_AB`, `1E50_CA`,
    `1EER_BA`, and `1EQZ_AB` as low-success target/protocol mismatches; keeps `1DXU_DC` as a positive
    control; and holds `1DQL_HL`, `1DSF_HL`, `1DVF_BA`, `1DXT_DC`, `1E44_BA`, `1E96_BA`, `1EAY_BD`,
    `1EM8_AB`, and `1EMV_BA` until a low-pAE acceptance or target-specific/family calibration strategy
    is predeclared. The next branch is now
    `w2_target_family_redesign_v11_post_v10_decision`, recorded in
    `results/m6d_w2_target_family_redesign_v11_followup_contract.{json,md}` and
    `configs/m6d_w2_target_family_redesign_v11_candidate_rules.json`. Cayuga submission remains
    `false` until at least three non-anchor replacement targets or a predeclared gate redesign pass strict
    manifest, MSA, completion-plan, and panel-report replay gates.
71. V11 no-spend replacement-target discovery and target-MSA prep are now complete, but panel submission
    has not been run:
    `results/m6d_w2_target_family_redesign_v11_seed_expansion.{json,md}` selected 160 new RCSB seeds
    after applying v11 exclusions. `results/m6d_w2_target_family_redesign_v11_discovery_pool.{json,md}`
    screened 2958 chain pairs and selected 20 source-diverse candidates. The full 20-target sequence audit
    is not broad-panel ready (`sequence_diversity_dominated_by_near_duplicates`, 7 clusters, largest
    cluster fraction 0.4), so the representative manifest
    `configs/m6d_w2_target_family_redesign_v11_representative_targets.json` keeps 7 cluster representatives:
    `10XZ_EF`, `10YB_GH`, `12NP_AH`, `10VB_IJ`, `10ZO_AB`, `1A2Y_BA`, and `1A6W_HL`.
    The representative audit
    `results/m6d_w2_target_family_redesign_v11_representative_sequence_diversity.{json,md}` passes with
    7/7 clusters and largest cluster fraction 0.142857. Target-MSA jobs `3073871`-`3073877` completed on
    Cayuga with exit code `0:0`; synced `.a3m` and report files pass local and remote
    `complex_target_manifest --require-files` with 7/7 ready targets.
    `results/m6d_w2_target_family_redesign_v11_readiness.json` is `ready`, and the regenerated raw submit
    plan `results/m6d_w2_target_family_redesign_v11_submit_panel.sh` writes to the corrected v11-specific
    records root `hpc_outputs/m6d_w2_target_family_redesign_v11_records/...`. The guarded wrapper
    `results/m6d_w2_target_family_redesign_v11_submit_with_receipt.sh` now delegates through the shared
    receipt-preserving wrapper, passes local/Cayuga dry-runs, and refuses real execution without
    `BIO_SFM_APPROVE_V11_PANEL=approve-v11-panel-submit`. Regenerate this approval-boundary state with
    `python -m bio_sfm_designer.experiments.m6d_w2_panel_guarded_preflight --run-local-dry-run`; it also
    emits the no-submit approval runbook, sync-back script, and panel-completion script for the approved-run
    aftermath. The sync-back script now fail-closes before any record `rsync` unless local submit
    receipt/summary, job-state probe output, and strict `m6d_w2_panel_postsubmit_status` with explicit
    `--manifest/--receipt/--summary/--job-states/--require-sync-ready/--out-json` all pass; project status
    records this as `panel_postsubmit_sync_ready_gate_ok=true`. The approval
    packet/runbook also records the full post-submit bridge: receipt-only monitor, one-command no-submit
    postsubmit driver, read-only job-state query, strict postsubmit status command, and post-sync replay.
    The public approval bundle, completion audit, submission-decision state, and local/Cayuga mirror audit
    require the no-submit pre-submit approval-intent audit command plus the exact driver/replay command pair:
    `bash results/m6d_w2_target_family_redesign_v11_postsubmit_driver.sh` followed by
    `bash results/m6d_w2_target_family_redesign_v11_postsync_interpretation.sh`. The job-state query
    script discovers job IDs from the submit receipt at runtime, fail-closes if the receipt is absent, and
    the approval bridge rsyncs the remote job-state probe JSON plus `sacct` TSV back locally before
    postsubmit status; project status records those invariants as `panel_job_state_query_bridge_ok=true`
    and `panel_postsubmit_bridge_ok=true`. The v11 panel approval packet, decision protocol,
    and remote-readiness audit report approval-ready/no-submit state
    and `can_claim_w2_generalization_now=false`; full project status now reports W2 as
    `panel_approval_packet_ready_awaiting_explicit_approval` and exposes the post-approval
    `resume_execution_ladder` from guarded submit through receipt monitor, job-state query, sync-ready
    status, sync-back, completion, and post-sync interpretation, including the non-approval phrase list used
    by goal-mode resumes. The no-submit remote readiness audit reports
    `remote_submission_readiness_ok` after 26 exact SHA, 7 semantic JSON, 2 receipt-absence, and
    10 shell syntax checks against
    Cayuga; project status also fail-closes if stored exact-check local SHA evidence no longer matches
    the current checkout. The final no-submit decision latch
    `results/m6d_w2_target_family_redesign_v11_submission_decision_state.{json,md}` now records
    `awaiting_explicit_panel_submission_approval`, `submitted=false`, local/remote receipt absence, and
    `can_claim_w2_generalization=false`; it also requires the completion audit's public approval bundle
    readiness, 9-step post-approval workflow, `script_chain_static_ok=true`, and 7-target/700-design/14-job
    approval scope before the decision can stay approval-ready. Its `operator_approval_checklist` binds the guarded submit
    entrypoint, postsubmit driver, post-sync replay, script-chain static sub-gates, local/remote receipt absence,
    700 planned designs, 14 expected Slurm jobs, and the explicit approval phrase in one operator-facing block. The public approval
    message itself can be checked, without submitting jobs, by running
    `python -m bio_sfm_designer.experiments.m6d_w2_v11_approval_intent_audit --message-file <approval-message.txt> --require-accepted`
    before any guarded submit command. The public approval
    bundle reads the tracked postsubmit driver, sync-back, completion, and post-sync replay scripts and
    fails closed unless their ordered chain reaches strict postsubmit status, sync-back, completion,
    target-wise report generation, decision refresh, and post-sync interpretation. Tracked result/status artifacts are public-safe and use
    placeholders for host, user, and repo-root values, while the executable Cayuga command bridge remains
    only in ignored local artifacts such as `results/m6d_w2_target_family_redesign_v11_approval_runbook.{json,md}`
    and `results/m6d_w2_target_family_redesign_v11_panel_approval_packet.json`; its approval-disambiguation block records that continuation phrases
    such as `resume goal`, `go ahead`, and `continue` are not approval. The post-submit status gate
    `results/m6d_w2_target_family_redesign_v11_postsubmit_status.{json,md}` currently records
    `not_submitted`; after explicit approval it validates the submit receipt/summary plus optional Slurm job
    states before allowing sync-back. The no-submit receipt monitor
    `results/m6d_w2_target_family_redesign_v11_receipt_monitor.{json,md}` currently records
    `receipt_absent_not_submitted`; after remote receipt creation it emits a receipt-only sync plan before
    any record sync-back. The companion no-submit job-state probe
    `results/m6d_w2_target_family_redesign_v11_job_state_probe.{json,md}` currently records
    `receipt_absent_not_submitted`; the emitted read-only `sacct` query plan is receipt-driven at runtime,
    writes postsubmit-compatible `states` JSON, and is synced back locally before record sync-back. The
    post-sync interpretation gate
    `results/m6d_w2_target_family_redesign_v11_postsync_interpretation.{json,md}` currently records
    `not_synced_not_interpretable`, emits the guarded replay path for sync-back -> completion ->
    `complex_panel_report` -> decision-protocol refresh, explicitly revalidates postsubmit status with the
    manifest, submit receipt/summary, and job-state JSON before sync-back, and keeps
    `can_claim_w2_generalization=false`.
    Do not reuse the earlier generated submit plan that pointed at
    `hpc_outputs/m6d_w2_fresh_discovery_records/...`.
    Cayuga default `python3` is Python 3.6.8 and fails on this repo; set `BIO_SFM_PYTHON`
    to the Boltz-capable Cayuga Python plus `PYTHONNOUSERSITE=1` for v11
    Cayuga replay/submission commands.

## W3 Decision Path

Current result: Chai-1 scale-up is complete and record quality is no longer the blocker.
The blocker is scientific disagreement:

- matched overlap: 30
- label agreement: 0.600
- required label agreement: 0.800
- both success: 18
- Boltz failure / Chai success: 12
- numeric-copy fraction: 0.0
- cross-predictor failure kinds: `label_agreement_below_min` only
- strict adjudication integrity: true; no target-identity/provenance/threshold/numeric-copy blockers

Chosen current W3 protocol: `adjudicated_disagreement_protocol_v1`.

Current protocol verdict: formalize the completed no-MSA Chai comparison as a
negative robustness result under that protocol. Do not rerun no-MSA Chai.
This negative result is valid only because the strict comparison integrity checks pass; if a future
cross-predictor report adds target-identity, provenance, label-threshold, overlap, or numeric-copy
blockers, W3 reverts to contract repair rather than adjudicated science.
The project status artifact consumes this decision through `--w3-decision-protocol`, reports
`negative_robustness_result_adjudicated`, and leaves W2 target-MSA replay as the only current
goal-mode requirement.
`results/m6d_goal_completion_audit.{json,md}` independently audits that boundary: it passes with
`audit_ok=true`, keeps `can_mark_goal_complete=false`, and names `W2_multi_target_panel` as the sole
remaining requirement. Its current W2 execution evidence includes the approved full-14 target-MSA path
synced back locally with strict `--require-files` passing plus the v11 panel approval/decision/remote-readiness
boundary; W2 remains incomplete until panel execution, sync-back, completion, and target-wise certification.
`results/m6d_local_cayuga_mirror_audit.{json,md}` independently audits local/Cayuga drift: 32 exact SHA
checks and 16 semantic JSON checks currently agree, including the v11 panel approval, remote-readiness,
submission-decision, receipt monitor, post-submit status, job-state probe, and post-sync interpretation
artifacts; the mirrored next action is explicit W2 v11 panel approval, then sync-back, completion, and
target-wise certification.
`results/m6d_goal_drift_audit.{json,md}` independently audits goal-boundary drift without submitting work:
current status is `no_major_direction_drift_w2_blocked`, `audit_ok=true`, `major_direction_drift=false`,
direction aligned, claim boundaries preserved, and execution recorded as
`panel_postsync_interpretation_predeclared_not_synced`. It also records
`current_state.W2_panel_submission_decision.operator_checklist_ok=true`,
`current_state.W2_panel_submission_decision.operator_script_chain_static_ok=true`, and
`current_state.completion_audit.panel_public_approval_bundle_workflow_script_chain_static_ok=true`,
and fails closed if either the operator checklist or script-chain gate drifts. The submission-decision latch likewise re-consumes the completion audit's
operator-checklist verdict, so a stale or incomplete checklist blocks approval-ready status instead of relying
only on the raw submission-decision artifact.
Runtime clarification checked on Cayuga: W2 production runs through the `boltz` env with Boltz-2 2.2.1;
the completed W3 comparator uses the separate `chai1` env with `chai_lab`/`chai-lab` 0.6.1. These are
separate predictor/protocol records and must not be pooled without the adjudication rule below.

If W3 is pursued further, use the deterministic adjudication set in
`results/m6d_w2_w3_decision_protocol.json` and the materialized full-row artifacts
`results/m6d_w3_adjudication_set.jsonl` / `results/m6d_w3_adjudication_set.json`
before more API/GPU spend. The current materialized set contains 12 discordant
Boltz/Chai labels and 6 concordant-success controls. Project status now verifies
that JSONL's sha256, row count, role counts, and target-id membership before
reporting W3 as `negative_robustness_result_adjudicated`.
`results/m6d_w3_adjudication_audit.{json,md}` now makes the same boundary independently reproducible:
it checks the decision protocol against the cross-predictor report, confirms label agreement 0.600 < 0.800,
verifies 18 materialized adjudication rows, and records `positive_claim_supported=false`.
`results/m6d_w3_next_protocol.{json,md}` now predeclares the next W3 science contract without spending
compute: status `w3_next_protocol_ready_no_spend`, `no_submit=true`, `no_api_spend=true`,
`no_gpu_spend=true`, and `can_claim_independent_predictor_robustness_now=false`. The primary next route is
`third_independent_predictor_or_protocol`; the secondary route is `stronger_chai_msa_template_protocol`,
which is treated as a protocol-variant branch rather than an independent-model closure. The challenge-panel
contract is 12 discordant Boltz/Chai rows plus 6 concordant-success controls, with 10/12 discordant
alignment and 5/6 control consistency needed before a full W3 matched-panel run is justified. That
challenge panel still is not a population-level robustness estimate and does not close the W3 caveat.
The main project status now consumes this contract through `--w3-next-protocol`, cross-checks it against
the adjudication JSONL path/sha/counts, and records `w3_next_protocol_ready=True`, `no_submit=True`,
`no_api_spend=True`, `no_gpu_spend=True`, and `can_claim_w3_now=False`.
`results/m6d_w3_challenge_manifest.{json,md}` now materializes the exact 18 selected adjudication rows as
a no-submit challenge-panel manifest. It confirms 12 discordant rows plus 6 concordant-success controls,
the pinned adjudication JSONL SHA-256, and full source-record coverage in both
`hpc_outputs/m6d_followup_3PC8_AB_scale_t030/records_boltz_complex_t030.jsonl` and
`hpc_outputs/m6c_second_predictor/chai1_complex_records.jsonl`. It records `execution_ready=false`
because the third predictor/protocol implementation, future execution inputs, and approved command wrapper
are still absent.
The main project status now also consumes this manifest through `--w3-challenge-manifest` and records
`w3_challenge_manifest_ready=True`, `execution_ready=False`, `no_submit=True`, and
`can_claim_w3_now=False`.
`results/m6d_w3_third_predictor_contract.{json,md}` now turns the challenge manifest into a no-submit
third-predictor execution contract. It pins the same 18 target IDs, requires future predictor/protocol
selection metadata, predeclares the result JSONL schema and strict QC checks, and names the future
artifacts required before execution. It emits no command wrapper or approval token, keeps
`execution_ready=false`, and does not support a positive robustness claim. The main project status now
consumes it through `--w3-third-predictor-contract` and records `w3_third_predictor_contract_ready=True`,
`execution_ready=False`, `wrapper_emitted=False`, and `can_claim_w3_now=False`.
`results/m6d_w3_predictor_selection_card.{json,md}` now selects
`af2_multimer_colabfold_v1` as the future W3 third predictor/protocol. The selection is deliberately
not execution-ready: prior Cayuga feasibility did not show `colabfold_batch`/AlphaFold installed in the
checked envs, so the next local artifact must probe or install the runtime and generate execution inputs
behind an approval gate. The card rejects the existing ESMFold path for this W3 complex adjudication
because it is single-chain/self-consistency oriented, keeps stronger Chai MSA/template as a secondary
protocol variant only, and defers AF3 until access/runtime/licensing are checked. Project status consumes
the card through `--w3-predictor-selection-card` and records `w3_predictor_selection_card_ready=True`,
`selected=af2_multimer_colabfold_v1`, `runtime_ready=False`, `execution_ready=False`, and
`can_claim_w3_now=False`.
`results/m6d_w3_runtime_probe_plan.{json,md}` now pins the no-submit runtime-probe contract for this
selected protocol. It records candidate ColabFold/localcolabfold runtime locations, Cayuga GPU-stack
checks, MSA-policy resolution, and dry-run enumeration requirements without executing any probe, emitting
an input manifest, emitting a command wrapper, or emitting an approval token. Project status consumes it
through `--w3-runtime-probe-plan` and records `w3_runtime_probe_plan_ready=True`,
`probe_executed=False`, `runtime_ready=False`, `execution_ready=False`, and `can_claim_w3_now=False`.
`results/m6d_w3_runtime_probe_report_local_static.{json,md}` preserves the first no-submit local-static
probe snapshot. It performs only local candidate discovery and dry-run enumeration of the 18-row
challenge panel. It does not SSH to Cayuga, submit jobs, query external MSA/API services, run ColabFold
prediction, emit execution inputs, or emit a command wrapper.
`results/m6d_w3_cayuga_runtime_probe_packet.{json,md}` now writes a syntax-checked no-submit probe script,
`results/m6d_w3_cayuga_runtime_probe_no_submit.sh`, for the target Cayuga probe surface. The packet
does not run prediction, submit scheduler jobs, query services, emit execution inputs, or emit a command
wrapper. The target Cayuga no-submit probe has now been executed once and recorded in
`results/m6d_w3_cayuga_runtime_probe_observed_checks.json` plus
`results/m6d_w3_runtime_probe_report.{json,md}`. Project status consumes that current report through
`--w3-runtime-probe-report` and records `w3_runtime_probe_report_ready=True`,
`surface=cayuga_gpu_no_submit`, `probe_executed=True`, `cayuga_probe=True`, `runtime_ready=False`,
`execution_ready=False`, and `can_claim_w3_now=False`. The failing checks are `env_discovery`,
`cli_help`, and `gpu_stack`; `msa_policy` and `dry_run_enumeration` pass.
`results/m6d_w3_cayuga_runtime_repair_discovery.json` records a read-only discovery on the configured
Cayuga login host: `colabfold_batch`/`localcolabfold_search` are absent, no `colabfold` conda env exists,
JAX/JAXLIB are absent from the checked `boltz`/`chai1` envs, and no module command is present.
`results/m6d_w3_runtime_repair_plan.{json,md}` consumes that discovery plus the Cayuga runtime-probe
report and records `w3_runtime_repair_plan_ready_no_submit`: provision a W3-specific ColabFold/JAX CUDA
runtime, then rerun the existing no-submit Cayuga runtime probe before generating execution inputs.
Project status consumes it through `--w3-runtime-repair-plan` and records
`w3_runtime_repair_plan_ready=True`, `failed_checks=cli_help,env_discovery,gpu_stack`,
`runtime_ready=False`, `execution_ready=False`, and `can_claim_w3_now=False`.
`results/m6d_w3_runtime_provision_packet.{json,md}` plus
`results/m6d_w3_runtime_provision_colabfold_guarded.sh` now define the next guarded runtime-validation
boundary. The packet is `w3_runtime_provision_packet_ready_no_submit`, requires
`BIO_SFM_APPROVE_W3_RUNTIME_PROVISION=approve-w3-runtime-provision`, accepts only an existing
`W3_COLABFOLD_BIN` or `W3_COLABFOLD_SIF`, and emits no download/install/scheduler/prediction path. The
no-env script check exits before receipt creation. Project status consumes it through
`--w3-runtime-provision-packet` and records `w3_runtime_provision_packet_ready=True`,
`runtime_ready=False`, `execution_ready=False`, and `can_claim_w3_now=False`.

1. Stage or point to an existing Cayuga ColabFold runtime, then run only the guarded runtime validation script after explicit approval.
2. Rerun the no-submit runtime probe for `af2_multimer_colabfold_v1` only after runtime validation succeeds; do not submit jobs or query external services.
3. Generate an execution-input manifest for the pinned 18-row challenge panel only after the Cayuga probe report sets `runtime_ready=true`.
4. Generate an approval-gated wrapper only after the runtime/input manifest is audited.
5. Rerun Chai under a stronger MSA/template protocol and treat no-MSA Chai as a protocol variant,
   not a final predictor verdict.
6. Keep the Boltz-Chai disagreement as a negative robustness result if no adjudication route is chosen.

## Run Contract

Use the local venv explicitly when generated bridge scripts invoke Python:

```sh
export BIO_SFM_PYTHON=/tmp/bio_sfm_venv/bin/python
export PYTHONNOUSERSITE=1
```

For Cayuga work, keep using the Cayuga checkout:

```text
${CAYUGA_BIO_SFM_HOST}:${CAYUGA_BIO_SFM_ROOT}
```

Do not submit new Cayuga jobs from stale readiness or status commands.

## Next Resume Command

Start with a no-spend status/decision refresh:

```sh
jq '{status, audit_ok, runtime_goal_active, w3: .w3_mechanism_completion.status, w3_outcome: .w3_mechanism_completion.joint_outcome, w3b: .w3b_successor.status, fit_scope: .w3b_successor.fit_scope, approval_recorded: .w3b_successor.approval_recorded, submitted_jobs: .w3b_successor.submitted_jobs, no_submit, cayuga_submission_allowed, next_actions_ranked, next_action}' \
  results/m6d_goal_state_refresh_report.json

jq '{status, audit_ok, completion_ok, n_targets, gpu_allocation_hours, within_gpu_budget, n_failures}' \
  results/m6d_w3b_target_msa_lifecycle.json

jq '{status, audit_ok, fit_packet_ready, explicit_fit_approval_recorded, submitted_jobs, can_submit_fit_stage, no_submit, next_action}' \
  results/m6d_w3b_fit_packet_readiness.json

jq '{status, audit_ok, approval_recorded, submitted_jobs, candidate_designs: .approval_contract.candidate_designs, matched_evaluations: .approval_contract.matched_predictor_evaluations, authorizes_certification: .approval_contract.authorizes_certification, authorizes_held_out_test: .approval_contract.authorizes_held_out_test, can_claim_w3b, no_submit}' \
  results/m6d_w3b_fit_approval_packet.json

git status --short --branch
```

Then choose the next science branch:

- W2 branch: `w2_target_family_redesign_v2_rcsb`, v3, v4, v5, and v7 are completed negative/evaluable for
  broad generalization. V8 is now completed and evaluable but not certified for broad W2 generalization:
  `results/m6d_w2_target_family_redesign_v8_panel_report.json` has 12 targets, 1200 records, and
  `panel_status=multi_target_evaluable_not_certified` at α=0.2. `1CG5_AB` and `1COH_BA` are target-specific
  certified controls; the other 10 targets are not certified. The pooled diagnostic certifies α=0.2, but
  pooled-only evidence is not W2 generalization. Before any more W2 panel GPU, use
  `results/m6d_w2_target_family_redesign_v8_redesign_diagnostic.{json,md}`: retain the two positives,
  replace low-success `1CA0_HI`/`1CBW_CD`, and predeclare a low-pAE acceptance or target-specific
  calibration strategy.
- W3 branch: third predictor, stronger Chai rerun, or formal negative robustness result.
