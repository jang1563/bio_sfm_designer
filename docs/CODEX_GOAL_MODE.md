# Codex Goal Mode

This is the operating contract for running `bio_sfm_designer` as a long-lived
Codex goal. It is intentionally not a publication plan. The job is to keep
developing the project as a research engine: stronger, more reproducible, more
honest about uncertainty, and harder to fool.

## Goal Objective

Use this objective when creating or resuming the Codex goal:

```text
Continue the bio_sfm_designer M6d science-result program in Cayuga-first goal mode:
redesign W2 multi-target generalization after the current evaluable-not-certified panel,
resolve W3 Boltz-Chai predictor disagreement through a chosen robustness protocol, preserve
W1 as target-specific certified evidence and W4 as closed-loop plumbing evidence, and keep
all status artifacts, tests, and local/Cayuga handoff anchors honest and reproducible.
```

## Current Resume Anchor

Current durable anchor: `docs/M6D_GOAL_MODE_ANCHOR.md`.

Current machine-readable decision artifact:
`results/m6d_goal_mode_current_anchor.json`.

Current W2/W3 decision protocol:
`results/m6d_w2_w3_decision_protocol.{json,md}`.

Current W3 next-protocol contract:
`results/m6d_w3_next_protocol.{json,md}`.

Current W3 challenge manifest:
`results/m6d_w3_challenge_manifest.{json,md}`.

Current W3 third-predictor execution contract:
`results/m6d_w3_third_predictor_contract.{json,md}`.

Current W3 predictor/protocol selection card:
`results/m6d_w3_predictor_selection_card.{json,md}`.

Current W3 runtime-probe plan:
`results/m6d_w3_runtime_probe_plan.{json,md}`.

Current W3 runtime-probe report:
`results/m6d_w3_runtime_probe_report.{json,md}`.

Current W3 local-static runtime-probe snapshot:
`results/m6d_w3_runtime_probe_report_local_static.{json,md}`.

Current W3 Cayuga runtime-probe packet:
`results/m6d_w3_cayuga_runtime_probe_packet.{json,md}` and
`results/m6d_w3_cayuga_runtime_probe_no_submit.sh`.

Current W3 Cayuga observed runtime checks:
`results/m6d_w3_cayuga_runtime_probe_observed_checks.json`.

Current W3 Cayuga runtime repair discovery:
`results/m6d_w3_cayuga_runtime_repair_discovery.json`.

Current W3 runtime repair plan:
`results/m6d_w3_runtime_repair_plan.{json,md}`.

Current W3 runtime provision packet:
`results/m6d_w3_runtime_provision_packet.{json,md}` and
`results/m6d_w3_runtime_provision_colabfold_guarded.sh`.

Current local harness status:
`results/m6d_goal_mode_local_harness_status.{json,md}`.

Current W2 v10 full-panel readout:
`results/m6d_w2_target_family_redesign_v10_panel_completion.json`,
`results/m6d_w2_target_family_redesign_v10_panel_report_alpha02.json`,
`results/m6d_w2_target_family_redesign_v10_panel_report_alpha03.json`, and
`results/m6d_w2_target_family_redesign_v10_redesign_diagnostic_alpha02.{json,md}`.

Current W2 v11 no-spend follow-up branch:
`results/m6d_w2_target_family_redesign_v11_followup_contract.{json,md}` and
`configs/m6d_w2_target_family_redesign_v11_candidate_rules.json`.

Historical W2 v9 target-MSA execution, guarded panel boundary, and post-panel decision protocol:
`results/m6d_w2_target_family_redesign_v9_full14_target_msa_execution_attempt.{json,md}`,
`results/m6d_w2_target_family_redesign_v9_panel_preflight.{json,md}`,
`results/m6d_w2_target_family_redesign_v9_panel_wrapper_guard_audit.{json,md}`,
`results/m6d_w2_target_family_redesign_v9_panel_approval_packet.{json,md}`, and
`results/m6d_w2_target_family_redesign_v9_panel_decision_protocol.{json,md}`.

Current W2 revised branch:
`results/m6d_w2_revised_branch.{json,md}`.

Current W2 candidate-pool screen:
`results/m6d_w2_candidate_pool_screen.{json,md}`.

Current W2 fresh-discovery pool:
`results/m6d_w2_fresh_discovery_pool.{json,md}`.

Current W2 fresh-discovery target manifest:
`configs/m6d_w2_fresh_discovery_complex_targets.json`.

Current W2 fresh-discovery target-MSA plan:
`results/m6d_w2_fresh_discovery_target_msas.sh`.

Current W2 fresh-discovery unique-source pilot:
`configs/m6d_w2_fresh_discovery_unique_source_pilot_targets.json`,
`results/m6d_w2_fresh_discovery_unique_source_pilot_manifest.json`, and
`results/m6d_w2_fresh_discovery_unique_source_pilot_submit.sh`.

Current W2 fresh-discovery unique-source pilot submit receipt:
`results/m6d_w2_fresh_discovery_unique_source_pilot_submit_receipt.json`.

Current W2 fresh-discovery unique-source pilot readout:
`results/m6d_w2_fresh_discovery_unique_source_pilot_completion.json`,
`results/m6d_w2_fresh_discovery_unique_source_pilot_panel_report.json`,
`results/m6d_w2_fresh_discovery_unique_source_pilot_diagnostic.{json,md}`, and the W2-only status snapshot
`results/m6d_project_status_w2_fresh_discovery_pilot.json`.

Current W2 next-branch design:
`results/m6d_w2_next_branch_design.{json,md}` and
`configs/m6d_w2_next_branch_candidate_rules.json`.

Current W2 next-branch candidate pool:
`results/m6d_w2_next_branch_candidate_pool.{json,md}`.

Current W2 next-branch manifest:
`results/m6d_w2_next_branch_manifest_design.{json,md}`,
`configs/m6d_w2_expanded_next_branch_targets.json`,
`results/m6d_w2_expanded_next_branch_manifest.json`,
`results/m6d_w2_expanded_next_branch_submit_plan.sh`, and
`results/m6d_w2_expanded_next_branch_submit_with_receipt.sh`, plus
`results/m6d_w2_expanded_next_branch_submit_wrapper_validation.json` and
`results/m6d_w2_expanded_next_branch_submission_status.json`, plus
`results/m6d_w2_expanded_next_branch_sync_back.sh` and
`results/m6d_w2_expanded_next_branch_completion.{json,sh}`.

Current W2 source-redundancy audit plan:
`results/m6d_w2_source_redundancy_audit_plan.{json,md}`.

Current W2 expanded-discovery branch:
`configs/m6d_w2_expanded_discovery_seed_rcsb_ids.json`,
`results/m6d_w2_expanded_discovery_pool.{json,md}`,
`configs/m6d_w2_expanded_discovery_complex_targets.json`,
`results/m6d_w2_expanded_discovery_targets_manifest_strict_pre_msa.json`, and
`results/m6d_w2_expanded_discovery_target_msas.sh`. The submitted target-MSA receipt is
`results/m6d_w2_expanded_discovery_target_msa_precompute_receipt.jsonl`; the post-job sync/replay plan is
`results/m6d_w2_expanded_discovery_sync_back.sh`.

Current science-action artifact:
`results/m6d_followup_next_science_actions.{json,md}`.

Current state:

- W1: certified target-specific complex gate evidence.
- W2: evaluable but not certified as multi-target generalization.
- Current W2 branch: v10 is completed negative evidence, and v11 is a no-spend follow-up fork.
  `results/m6d_w2_target_family_redesign_v11_followup_contract.json` and
  `configs/m6d_w2_target_family_redesign_v11_candidate_rules.json` define the fork. The v11 no-spend
  replacement-target discovery has advanced through target-MSA prep: 20 structural candidates were found,
  the full 20-target sequence panel was near-duplicate dominated, a 7-target representative manifest
  passed diversity, target-MSA jobs `3073871`-`3073877` completed on Cayuga, and
  `results/m6d_w2_target_family_redesign_v11_readiness.json` is `ready`. The raw panel submit plan is
  corrected to write records under `hpc_outputs/m6d_w2_target_family_redesign_v11_records/...`; the guarded
  wrapper `results/m6d_w2_target_family_redesign_v11_submit_with_receipt.sh` passes local/Cayuga dry-runs,
  preserves the receipt path, and refuses real execution without
  `BIO_SFM_APPROVE_V11_PANEL=approve-v11-panel-submit`. Regenerate the wrapper/preflight with
  `python -m bio_sfm_designer.experiments.m6d_w2_panel_guarded_preflight --run-local-dry-run`; this also
  emits `results/m6d_w2_target_family_redesign_v11_approval_runbook.{json,md}`,
  `results/m6d_w2_target_family_redesign_v11_sync_back.sh`, and
  `results/m6d_w2_target_family_redesign_v11_panel_completion.sh`. The emitted approval packet plus
  `results/m6d_w2_target_family_redesign_v11_panel_decision_protocol.{json,md}` records the current W2
  decision as ready for explicit approval, still `no_submit=true`, and not W2 evidence; full project status now
  reports W2 as `panel_approval_packet_ready_awaiting_explicit_approval`. The no-submit remote readiness
  audit (`python -m bio_sfm_designer.experiments.m6d_w2_v11_remote_submission_readiness`) reports
  `remote_submission_readiness_ok` with 14 exact SHA checks, 5 semantic JSON checks, and 2 receipt-absence
  checks. It has not been submitted.
- Current known W2 candidate pool: screened 12 known local targets, including the completed fresh
  unique-source pilot targets, admitted 0 for pilot,
  and does not authorize a revised manifest or Cayuga submission.
- Latest W2 v10 target-family panel: 15/15 Cayuga targets completed and synced back. The α=0.2 report
  is `multi_target_evaluable_not_certified` with 15 targets and 1500 records; the pooled diagnostic
  certifies α=0.2, but pooled-only evidence remains diagnostic, and only `1DXU_DC` certifies target-wise.
  The α=0.3 diagnostic remains `multi_target_evaluable_not_certified` with four target-wise controls
  (`1DXU_DC`, `1E44_BA`, `1EM8_AB`, `1EMV_BA`) and 11 targets still not certified.
  The v10 diagnostic marks `13IO_HC`, `1DS6_AB`, `1E50_CA`, `1EER_BA`, and `1EQZ_AB` as low-success
  target/protocol mismatches. Do not submit another W2 panel until the v11 no-spend branch admits at
  least three replacement targets or a target/family calibration redesign passes strict preflight.
- Current W2 fresh-discovery pool: screened 10 public RCSB seeds, selected 6 structural chain-pair
  candidates from 3 unique source PDBs. Target-MSA precompute has completed for all 6 candidates, and the
  full fresh manifest now passes strict `--require-files`. To avoid source-redundancy drift, the current
  pilot was the 3-target unique-source set (`1FQJ_EB`, `1AK4_BC`, `1A2K_CB`), not the full
  6-chain-pair inventory. That pilot completed on Cayuga and synced back 300 records. The readout is
  `multi_target_evaluable_not_certified` at `alpha=0.2`: all three targets are `not_certified`,
  `trusted=0`, and no `tau` exists. The fresh-pilot diagnostic classifies all three as
  `target_protocol_mismatch_low_success`, with zero accepts under the transferred t0.3 low-pAE cutoff.
- Current W2 next-branch design: `protocol_redesign_plus_success_enriched_discovery_v1` is selected;
  `configs/m6d_w2_next_branch_candidate_rules.json` is emitted as a no-spend unlock rule set, with
  `spend_gate.cayuga_submission_allowed=false`.
- Current W2 next-branch candidate pool: 25 local inventory candidates screened after adding the expanded
  discovery manifest. After expanded target-MSA sync-back, 10 are admitted for the next W2 branch,
  3 remain source-redundancy audit-only (`1A2K_DA`, `1A2K_EB`, `1AK4_AD`), and 0 remain target-MSA
  blocked. This opens revised manifest design, not a W2 evidence claim.
- Current W2 source-redundancy audit plan: written for `1A2K_DA`, `1A2K_EB`, and `1AK4_AD`, but it is
  `ready_for_cayuga_submission=false` and `ready_for_w2_generalization_claim=false`.
- Current W2 expanded-discovery branch: screened 49 seed PDBs and 248 chain pairs, selected 10
  source-diverse candidates from 10 unique source PDBs (`1BVK`, `1CHO`, `1EER`, `1HE1`, `1KAC`, `1KLU`,
  `1MLC`, `1NMB`, `1QFW`, `2MTA`). The first target-MSA batch had seven ColabFold MSA-server failures,
  but the low-concurrency retry completed the remaining targets. The combined audit is now
  `target_msa_precompute_complete_after_retry`, 10/10 target `.a3m`/report files are synced locally,
  and strict `--require-files` passes.
- Current W2 expanded next-branch manifest: `configs/m6d_w2_expanded_next_branch_targets.json` freezes
  the 10 admitted targets. `results/m6d_w2_expanded_next_branch_manifest.json` has `ok=true`,
  `n_ready_targets=10`, and no failures. The expanded panel has completed and synced back:
  `results/m6d_w2_expanded_next_branch_completion.json` is `ready_for_panel_report` with 10/10 completed
  targets, and `results/m6d_w2_expanded_next_branch_panel_report.json` is
  `multi_target_evaluable_not_certified` at alpha=0.2 with 10 targets and 1000 records. Original Boltz
  job `3056576` for `1QFW_BA` failed on a target-MSA/candidate sequence mismatch caused by a terminal
  atom-only residue, but repair job `3056582` completed using CA-target-sequence repaired candidates.
  This authorizes no W2 generalization claim: all 10 target-wise certificates are `not_certified`, even
  though the pooled diagnostic can certify tau=0.4. This branch was later superseded by the v9 and v10
  target-family redesign sequence; the active W2 fork is now v11 no-spend follow-up.
  `results/m6d_w2_target_family_redesign_v9_target_msa_gate_audit.{json,md}` is a historical no-spend
  checkpoint: `audit_ok=true`, 14 representative targets, 28 expected pending target-MSA/MSA-report paths,
  70/98 input-prep artifacts present, and `ready_for_panel_submission=false`.
  `results/m6c_project_status_w2_followup.json` consumes this audit as superseded target-MSA-gate
  provenance; after the synced target-MSA execution and panel approval packet, it now reports W2 as
  `panel_approval_packet_ready_awaiting_explicit_approval`.
  `results/m6d_w2_target_family_redesign_v9_approval_packet.{json,md}` is the current no-submit
  approval packet and is ready only for target-MSA input prep after explicit approval. Project status
  consumes it through `--w2-approval-packet` and keeps panel submission blocked.
  `results/m6d_w2_target_family_redesign_v9_wrapper_guard_audit.{json,md}` proves the wrapper boundary
  without submitting: static audit passes, no-env execution exits before receipt initialization, and the
  v9 target-MSA receipt remains absent locally and on Cayuga.
  `results/m6d_w2_explicit_approval_runbook.{json,md}` is the no-submit runbook to use only after explicit
  approval; it records the target-MSA-only command order and keeps panel submission blocked until sync-back
  plus strict replay pass.
  The postsubmit replay plan records the same approval-env-gated target-MSA command as the presubmit
  preflight, and the gate audit now fails if those commands drift.
	  `results/m6d_w2_target_family_redesign_v9_approval_parity.{json,md}` confirms local/Cayuga packet
	  agreement without creating a target-MSA receipt; project status consumes it through
	  `--w2-approval-parity`.
- Current local science harness: `results/m6d_goal_mode_local_harness_status.{json,md}` records
  `active_local_harness_stabilized`. The latest full local WIP suite run is `676 passed` using
  `/tmp/bio_sfm_science_venv/bin/python -m pytest -q`. The repaired local blockers were generated
  shell plans assuming bare `python`, shared `subprocess.run` monkeypatch leakage in HPC contract tests,
  a second-predictor missing-file readiness test that depended on repo-root fixture state, stale
  project-status/audit wording around the new W2 panel approval packet, W3 runtime-probe provenance
  after the target Cayuga no-submit probe, W3 runtime repair-plan consumption, and W3 guarded runtime
  provision-packet consumption. The current addition records the Cayuga probe as runtime-blocked and emits
  a guarded runtime validation packet, not execution inputs.
- Historical W2 v9 execution attempt: after explicit full-14 target-MSA input-prep approval, the configured
  Cayuga login host became reachable and the target-MSA-only wrapper ran successfully.
  `results/m6d_w2_target_family_redesign_v9_full14_target_msa_execution_attempt.{json,md}` reports
  `target_msa_outputs_synced_strict_require_files_passed`, latest check `2026-07-05T09:08:12+09:00`.
  The first pass submitted jobs `3059563`-`3059576`; 11 completed and 3 failed with the known
  ColabFold/MMseqs truncated tar/gzip response. The same audited target-MSA wrapper retried only those
  3 targets as jobs `3059577`-`3059579`, all completed. Sync-back then passed: `98/98` input-prep
  artifacts, strict manifest `14/14` targets ready, post-sync pending paths `0`. This is target-MSA
  input-prep provenance only, not W2 evidence and not ProteinMPNN/Boltz panel submission.
- Historical W2 v9 panel preflight: `results/m6d_w2_target_family_redesign_v9_panel_preflight.{json,md}`
  records `panel_preflight_dry_run_passed_not_submitted`. The generator emitted
  `results/m6d_w2_target_family_redesign_v9_submit_ready.json` and
  `results/m6d_w2_target_family_redesign_v9_submit_plan.sh`; branch-specific workflow entrypoints are
  `results/m6d_w2_target_family_redesign_v9_submit_with_receipt.sh`,
  `results/m6d_w2_target_family_redesign_v9_completion.sh`, and
  `results/m6d_w2_target_family_redesign_v9_sync_back.sh`. Dry-run verified `14/14` targets, made no
  `sbatch` calls, and left the v9 submit receipt/summary absent. This is still not W2 panel evidence.
- Historical W2 v9 panel approval boundary:
  `results/m6d_w2_target_family_redesign_v9_panel_wrapper_guard_audit.{json,md}` reports
  `panel_wrapper_guard_ok`: non-dry execution without
  `BIO_SFM_APPROVE_V9_PANEL=approve-v9-panel-submit` exits before submit receipt creation.
  `results/m6d_w2_target_family_redesign_v9_panel_approval_packet.{json,md}` reports
  `panel_approval_packet_ready`, `can_submit_panel_if_user_explicitly_approves=true`, and
  `can_claim_w2_generalization=false`. The packet fixes the guarded Cayuga submit command and sync-back
  command, but it is not approval and it is not W2 evidence. Project status consumes it through
  `--w2-panel-approval-packet` while keeping W2 incomplete.
- Historical W2 v9 post-panel decision protocol:
  `results/m6d_w2_target_family_redesign_v9_panel_decision_protocol.{json,md}` reports
  `post_panel_decision_protocol_ready`, `no_submit=true`, and `can_claim_w2_generalization_now=false`.
  It predeclares the alpha=0.2, 14-target decision rules for interpreting future synced panel records:
  completion/record-contract failures produce no claim; all target-wise certificates are required for a
  W2 generalization claim; partial certificates remain target-specific only; pooled diagnostic success
  cannot override target-wise failure.
- Goal-boundary drift is now independently audited by
  `results/m6d_goal_drift_audit.{json,md}`. Current status is
  `no_major_direction_drift_w2_blocked`, with `major_direction_drift=false`, direction aligned,
  claim boundaries preserved, and execution recorded as `panel_remote_readiness_ready_not_submitted`.
- W3: Chai-1 records exist and pass QC/contract, but Boltz-Chai label agreement is 0.600
  against the required 0.800, so independent-predictor robustness is not supported. This is
  treated as a negative no-MSA Chai robustness result only because strict adjudication integrity
  passes: the sole cross-predictor failure kind is `label_agreement_below_min`, with no
  target-identity, provenance, label-threshold, overlap, or numeric-copy blockers.
  `results/m6d_w3_adjudication_set.jsonl` and `results/m6d_w3_adjudication_set.json`
  materialize the next allowed W3 adjudication input set: 12 discordant Boltz/Chai labels plus
  6 concordant-success controls.
  Project status now consumes `results/m6d_w2_w3_decision_protocol.json` through
  `--w3-decision-protocol`, verifies the materialized JSONL sha256/count/roles, and reports W3 as
  `negative_robustness_result_adjudicated`; this does not authorize a positive independent-predictor
  robustness claim.
  `results/m6d_w3_adjudication_audit.{json,md}` is the standalone no-spend audit for this boundary:
  label agreement 0.600 < 0.800, 18 adjudication rows, sha/count/role/membership checks passing, and
  `positive_claim_supported=false`.
  `results/m6d_w3_next_protocol.{json,md}` is the current no-submit W3 next-protocol contract:
  status `w3_next_protocol_ready_no_spend`, no API/GPU spend, primary next route
  `third_independent_predictor_or_protocol`, secondary route `stronger_chai_msa_template_protocol`.
  Its challenge-panel contract requires 10/12 discordant-row alignment plus 5/6 control consistency
  before the next W3 branch can justify a full matched-panel run; it still does not authorize a positive
  independent-predictor robustness claim.
  Project status now consumes this artifact through `--w3-next-protocol`, cross-checks it against the
  adjudication JSONL row/count/sha contract, and reports
  `w3_next_protocol_ready=True no_submit=True no_api=True no_gpu=True can_claim_w3_now=False`.
  `results/m6d_w3_challenge_manifest.{json,md}` materializes the 18 selected rows as a no-submit
  challenge-panel manifest: 12 discordant Boltz/Chai labels plus 6 concordant-success controls, with both
  source record files covering all 18 selected target IDs. It records `execution_ready=false` because no
  third-predictor/protocol implementation, execution inputs, or approved command wrapper are emitted.
  Project status now also consumes this artifact through `--w3-challenge-manifest` and reports
  `w3_challenge_manifest_ready=True execution_ready=False no_submit=True can_claim_w3_now=False`.
  `results/m6d_w3_third_predictor_contract.{json,md}` now pins the third-predictor execution contract
  over the same 18 rows: required predictor/protocol selection fields, expected output JSONL schema,
  strict QC rules, future artifacts required, and the approval-gated wrapper requirement. It remains
  no-submit/no-API/no-GPU, emits no command wrapper or approval token, and cannot support a positive W3
  robustness claim. Project status consumes it through `--w3-third-predictor-contract` and reports
  `w3_third_predictor_contract_ready=True execution_ready=False wrapper_emitted=False can_claim_w3_now=False`.
  `results/m6d_w3_predictor_selection_card.{json,md}` selects
  `af2_multimer_colabfold_v1` as the future third predictor/protocol because it is complex-specific and
  independent from the Boltz/Chai comparison, while the repo ESMFold path is single-chain and stronger
  Chai remains only a protocol-variant branch. This selection card is still no-submit/no-API/no-GPU:
  runtime probe, execution inputs, and approval-gated wrapper are all absent. Project status consumes it
  through `--w3-predictor-selection-card` and reports
  `w3_predictor_selection_card_ready=True selected=af2_multimer_colabfold_v1 runtime_ready=False execution_ready=False can_claim_w3_now=False`.
  `results/m6d_w3_runtime_probe_plan.{json,md}` now pins the no-submit runtime-probe contract for that
  selected protocol: candidate ColabFold/localcolabfold runtime locations, Cayuga GPU-stack checks,
  MSA-policy resolution, and dry-run enumeration requirements. It executes no probe, emits no execution
  inputs, emits no command wrapper or approval token, and keeps `runtime_ready=false`,
  `execution_ready=false`, and `can_claim_w3_now=false`. Project status consumes it through
  `--w3-runtime-probe-plan` and reports
  `w3_runtime_probe_plan_ready=True probe_executed=False runtime_ready=False execution_ready=False can_claim_w3_now=False`.
  `results/m6d_w3_runtime_probe_report_local_static.{json,md}` preserves the first safe local-static
  no-submit probe snapshot. It performs local candidate discovery and 18-input enumeration only; it does
  not SSH to Cayuga, submit jobs, query MSA/API services, run ColabFold prediction, emit execution inputs,
  or emit a command wrapper.
  `results/m6d_w3_cayuga_runtime_probe_packet.{json,md}` now writes
  `results/m6d_w3_cayuga_runtime_probe_no_submit.sh`, a syntax-checked no-submit script for the target
  Cayuga probe surface. The packet itself does not run prediction, submit scheduler jobs, query services,
  emit execution inputs, or emit a command wrapper.
  The target Cayuga no-submit probe has now been executed once and recorded in
  `results/m6d_w3_cayuga_runtime_probe_observed_checks.json` plus
  `results/m6d_w3_runtime_probe_report.{json,md}`. Project status consumes that current report through
  `--w3-runtime-probe-report` and reports
  `w3_runtime_probe_report_ready=True surface=cayuga_gpu_no_submit probe_executed=True cayuga_probe=True runtime_ready=False execution_ready=False can_claim_w3_now=False`.
  The failing runtime checks are `env_discovery`, `cli_help`, and `gpu_stack`; `msa_policy` and
  `dry_run_enumeration` pass. Next W3 action is runtime repair plus a rerun of the same no-submit probe,
  not execution-input generation.
  `results/m6d_w3_cayuga_runtime_repair_discovery.json` records a read-only discovery on the configured
  Cayuga login host: `colabfold_batch`/`localcolabfold_search` are absent, no `colabfold` conda env exists,
  JAX/JAXLIB are absent from the checked `boltz`/`chai1` envs, and no module command is present.
  `results/m6d_w3_runtime_repair_plan.{json,md}` consumes that discovery plus the Cayuga runtime-probe
  report and records `w3_runtime_repair_plan_ready_no_submit`: provision a W3-specific ColabFold/JAX CUDA
  runtime, then rerun the existing no-submit Cayuga runtime probe before generating execution inputs.
  Project status consumes it through `--w3-runtime-repair-plan` and reports
  `w3_runtime_repair_plan_ready=True failed_checks=cli_help,env_discovery,gpu_stack runtime_ready=False execution_ready=False can_claim_w3_now=False`.
  `results/m6d_w3_runtime_provision_packet.{json,md}` and
  `results/m6d_w3_runtime_provision_colabfold_guarded.sh` now define the guarded validation boundary for
  a future ColabFold runtime. The generated script requires
  `BIO_SFM_APPROVE_W3_RUNTIME_PROVISION=approve-w3-runtime-provision`, accepts only an existing
  `W3_COLABFOLD_BIN` or `W3_COLABFOLD_SIF`, and contains no download/install/scheduler/prediction path.
  Project status consumes it through `--w3-runtime-provision-packet` and reports
  `w3_runtime_provision_packet_ready=True runtime_ready=False execution_ready=False can_claim_w3_now=False`.
- W4: closed-loop DBTL plumbing is complete in fail-closed/all-defer mode, not productive
  build-selection evidence.

Do not resume from the older instruction to "scale matched Chai records"; that scale-up is done.
Do not resume from the older W2 v9 submit boundary either; v9 and v10 panel execution are now completed
negative/evaluable evidence. Resume from the W2 v11 representative-panel boundary unless the user explicitly
selects the W3 decision fork. The next W2 boundary is an explicit decision on whether to run the 7-target
v11 ProteinMPNN/Boltz panel from `results/m6d_w2_target_family_redesign_v11_submit_panel.sh`; do not claim
W2 generalization unless a future target-wise panel certificate passes. The W3
next protocol is now predeclared: third independent predictor/protocol first, stronger Chai MSA/template
as a protocol-variant branch. The current W3 Cayuga probe has executed but is runtime-blocked; repair
ColabFold/JAX/GPU availability through the guarded runtime provision-packet boundary, then rerun the
no-submit probe before generating execution inputs.

## Non-Negotiables

- Do not drift into publication, slides, or external narrative unless explicitly asked.
- Do not let Claude or any orchestrator own trust. Trust is owned by the external calibrated gate.
- Do not claim generalization from pooled evidence. Multi-target evidence must be target-wise.
- Do not close the single-predictor caveat until an independent complex predictor passes the contract
  and cross-predictor checks.
- Do not spend or request GPU/HPC work from stale commands. Refresh readiness/status first.
- Do not bypass strict QC, provenance, target identity, chain ids, or alpha-mismatch guards.
- Treat negative results, refusals, and quantified underpowering as useful progress.

## Turn-Start Checklist

At the start of each goal-mode turn:

1. Read or skim these anchors:
   - `HANDOFF.md`
   - `docs/PROJECT_ROADMAP.md`
   - `docs/M6D_GOAL_MODE_ANCHOR.md`
   - this file
   - `results/m6d_goal_mode_local_harness_status.{json,md}`
   - `results/m6d_w2_target_family_redesign_v9_full14_target_msa_execution_attempt.{json,md}`
   - `results/m6d_w2_candidate_pool_screen.{json,md}`
   - `results/m6d_w2_next_branch_design.{json,md}`
   - `configs/m6d_w2_next_branch_candidate_rules.json`
   - `results/m6d_w2_next_branch_candidate_pool.{json,md}`
   - `results/m6d_w2_next_branch_manifest_design.{json,md}`
   - `configs/m6d_w2_expanded_next_branch_targets.json`
   - `results/m6d_w2_expanded_next_branch_manifest.json`
   - `results/m6d_w2_expanded_next_branch_submit_plan.sh`
   - `results/m6d_w2_expanded_next_branch_submit_with_receipt.sh`
   - `results/m6d_w2_expanded_next_branch_completion.{json,sh}`
   - `results/m6d_w2_source_redundancy_audit_plan.{json,md}`
   - `results/m6d_w2_expanded_discovery_pool.{json,md}`
   - `results/m6d_w2_expanded_discovery_targets_manifest_strict_pre_msa.json`
   - `results/m6d_w2_expanded_discovery_target_msas.sh`
   - `results/m6d_w2_expanded_discovery_target_msa_precompute_receipt.jsonl`
   - `results/m6d_w2_expanded_discovery_target_msa_job_audit.{json,md}`
   - `results/m6d_w2_expanded_discovery_sync_back.sh`
   - `results/m6d_w2_fresh_discovery_pool.{json,md}`
   - `results/m6d_w2_fresh_discovery_targets_manifest.json`
   - `results/m6d_w2_fresh_discovery_unique_source_pilot_manifest.json`
   - `results/m6d_w2_fresh_discovery_unique_source_pilot_submit_receipt.json`
   - `results/m6d_w2_fresh_discovery_unique_source_pilot_completion.json`
   - `results/m6d_w2_fresh_discovery_unique_source_pilot_panel_report.json`
   - `results/m6d_w2_fresh_discovery_unique_source_pilot_diagnostic.{json,md}`
   - `results/m6d_project_status_w2_fresh_discovery_pilot.json`
   - `results/m6d_w2_revised_branch.{json,md}`
	   - `results/m6d_w2_w3_decision_protocol.{json,md}`
	   - `results/m6d_goal_drift_audit.{json,md}`
	   - `results/m6d_followup_next_science_actions.{json,md}`
	   - the latest relevant `results/*readiness*.json`
	   - the latest relevant `results/*project_status*.json`
2. Check current git status without reverting unrelated work.
3. Refresh the live readiness artifact before choosing expensive or stateful work.
4. Let the JSON artifacts choose the next action. Do not choose by intuition when a status artifact
   already names a blocker.

Current W2 fresh-discovery full-manifest refresh:

```sh
python -m bio_sfm_designer.experiments.complex_target_manifest \
  --manifest configs/m6d_w2_fresh_discovery_complex_targets.json \
  --min-targets 3 \
  --min-contacts 20 \
  --require-files \
  --out results/m6d_w2_fresh_discovery_targets_manifest.json \
  --emit-msa-plan results/m6d_w2_fresh_discovery_target_msas.sh \
  --max-failures 100
```

Current W2 unique-source pilot refresh:

```sh
python -m bio_sfm_designer.experiments.complex_target_manifest \
  --manifest configs/m6d_w2_fresh_discovery_unique_source_pilot_targets.json \
  --min-targets 3 \
  --min-contacts 20 \
  --require-files \
  --out results/m6d_w2_fresh_discovery_unique_source_pilot_manifest.json \
  --emit-plan results/m6d_w2_fresh_discovery_unique_source_pilot_submit.sh \
  --max-failures 100
```

Current W2 expanded-discovery target-MSA plan refresh:

```sh
python -m bio_sfm_designer.experiments.complex_target_manifest \
  --manifest configs/m6d_w2_expanded_discovery_complex_targets.json \
  --min-targets 3 \
  --min-contacts 20 \
  --out results/m6d_w2_expanded_discovery_targets_manifest_pre_msa.json \
  --emit-msa-plan results/m6d_w2_expanded_discovery_target_msas.sh \
  --max-failures 100

python -m bio_sfm_designer.experiments.complex_target_manifest \
  --manifest configs/m6d_w2_expanded_discovery_complex_targets.json \
  --min-targets 3 \
  --min-contacts 20 \
  --require-files \
  --out results/m6d_w2_expanded_discovery_targets_manifest_strict_pre_msa.json \
  --max-failures 100
```

Current M6D readiness refresh:

```sh
python -m bio_sfm_designer.experiments.complex_readiness \
  --target-manifest configs/m6d_candidate_complex_targets.json \
  --input-prep-completion results/m6d_candidate_input_prep_completion.json \
  --require-files \
  --panel-min-targets 3 \
  --target-alpha 0.2 \
  --out results/m6d_candidate_readiness.json \
  --emit-plan results/m6d_candidate_readiness.sh
```

Current roadmap-status refresh:

```sh
python -m bio_sfm_designer.experiments.complex_project_status \
  --posthoc-manifest results/m6c_posthoc/manifest.json \
  --scale-completion results/m6c_scale_completion_1BRS_AD.json \
  --scale-input-prep-completion results/m6c_input_prep_completion.json \
  --scale-target-manifest configs/template_complex_targets.json \
  --scale-readiness-report results/m6c_readiness.json \
  --target-manifest-report results/m6d_candidate_targets_manifest.json \
  --panel-input-prep-completion results/m6d_candidate_input_prep_completion.json \
  --panel-target-manifest configs/m6d_candidate_complex_targets.json \
  --panel-readiness-report results/m6d_candidate_readiness.json \
  --panel-completion results/m6c_panel_completion.json \
  --predictor-contract-report results/m6c_second_predictor_contract.json \
  --predictor-sync-back-plan results/m6c_second_predictor_sync_back.sh \
  --batch-preflight results/m6c_w4_round/preflight.json \
  --batch-summary results/m6c_w4_round/summary.json \
  --batch-campaign results/m6c_w4_round/campaign.jsonl \
  --batch-sync-back-plan results/m6c_w4_sync_back.sh \
  --target-alpha 0.2 \
  --out results/m6c_project_status.json \
  --emit-pending-input-prep-paths results/m6c_project_pending_input_prep_paths.txt \
  --emit-pending-external-paths results/m6c_project_pending_external_paths.txt \
  --emit-sync-back-plan results/m6c_project_sync_back.sh \
  --emit-external-sync-back-plan results/m6c_project_external_sync_back.sh \
  --emit-external-remote-check-plan results/m6c_project_remote_check.sh \
  --external-remote-check-report results/m6c_project_remote_check.json \
  --emit-target-msa-precompute-plan results/m6c_project_target_msa_precompute.sh \
  --emit-post-sync-plan results/m6c_project_post_sync.sh
```

## Priority Order

Use this order unless the user gives a newer instruction:

1. Keep the local harness reproducible: tests, replay scripts, completion checks, status reports,
   and documentation that prevents stale HPC actions.
2. W1 - Preserve the certified target-specific evidence. Do not spend more W1 GPU unless a
   new alpha target or target-specific stress test is explicitly chosen.
3. W2 - Redesign multi-target generalization. Treat the current panels as completed negative
   evidence, then select or design more 3PC8-like targets before any broad panel spend.
4. W3 - Resolve predictor disagreement. The Chai scale-up is complete; choose a third
   predictor/protocol, stronger Chai MSA/template rerun, or formal negative robustness result.
5. W4 - Preserve closed-loop DBTL plumbing evidence. Do not describe it as productive
   build-selection evidence until the screen head/verdict quality changes.
6. W5/W6 - De-novo binders and live orchestration only after the trust-gate, safety, provenance,
   and key-rotation constraints are satisfied.

If the current readiness artifact exposes a runnable blocker outside this priority order, resolve that
blocker first. For example, if M6D says `waiting_on_input_prep`, focus on the target-MSA precompute,
sync checklist, and completion/readiness replay before starting unrelated W3 or W4 work.

## Blocked-State Policy

When Cayuga or another external system is required and cannot be accessed from the local session:

- Do not mark the project complete.
- Do not invent results or edit claims around missing records.
- Continue local progress only if it reduces future Cayuga risk, for example:
  - make pending-file lists machine-readable;
  - preserve replay flags in emitted shell plans;
  - add completion checks before posthoc/report tools;
  - make status artifacts explain the earliest blocker;
  - add tests for the blocker so it cannot silently regress;
  - update runbooks with exact commands and sync-back paths.
- If no meaningful local progress remains, end the turn with the exact external command and files needed.
- Only mark the Codex goal as blocked after the same external blocker repeats across resumed turns and
  there is no useful local hardening left.

Completed external M6D anchor for W2 fresh-discovery target-MSA prep:

```sh
bash results/m6d_w2_fresh_discovery_target_msas.sh
```

This submitted only target-MSA precompute jobs for the fresh-discovery candidates. Jobs
`3056468`-`3056473` completed and the target `.a3m` plus `.a3m.report.json` files were synced back.

Completed external M6D anchor for the W2 unique-source pilot:

```sh
BIO_SFM_PYTHON="${BIO_SFM_PYTHON:-$HOME/.conda/envs/chai1/bin/python}" \
  bash results/m6d_w2_fresh_discovery_unique_source_pilot_submit.sh
```

This was the first ProteinMPNN/Boltz pilot submit candidate for the fresh-discovery branch. It is not a W2
generalization claim. Records synced back for all three targets, completion passed, and the panel report is
`multi_target_evaluable_not_certified` at `alpha=0.2` with `trusted=0` and `tau=null`.

Completed W2 pilot job IDs:

```text
1FQJ_EB: ProteinMPNN 3056479 -> Boltz 3056480
1AK4_BC: ProteinMPNN 3056481 -> Boltz 3056482
1A2K_CB: ProteinMPNN 3056483 -> Boltz 3056484
```

Older external M6D anchor for the prior candidate panel:

```sh
bash results/m6d_candidate_target_msas.sh
```

After the Cayuga target-MSA plan finishes, pull the pending files and replay local checks:

```sh
export CAYUGA_BIO_SFM_ROOT=NETID@cayuga:/scratch/NETID/bio_sfm_designer
bash results/m6c_project_remote_check.sh
# Rerun the full "Current roadmap-status refresh" command above.
# It should include: --external-remote-check-report results/m6c_project_remote_check.json
bash results/m6c_project_external_sync_back.sh
```

## Artifact Decision Rules

- `complex_readiness.py` is the pre-spend gate. It should order source prep, target-MSA precompute,
  scale or panel submission, completion checks, posthoc refresh, predictor follow-up, W4 routing, and
  status refresh.
- `complex_input_prep_completion.py` is the sync-back gate for source/prepared PDB, FASTA/MSA, and
  report files. Its `pending_artifacts`, `blocked_targets`, `artifacts_by_target`, and optional
  `--emit-pending-paths` output are the copy checklist.
- `complex_project_status.py --emit-sync-back-plan results/m6c_project_sync_back.sh` writes the W1/W2
  input-prep pull bridge; per-path `rsync` steps and the post-sync call are failure-collected so a
  partial remote sync still reaches later pulls and local replay, then exits nonzero if anything failed.
- `complex_scale_completion.py` is the sync-back gate before W1 posthoc.
- `complex_panel_completion.py` is the sync-back gate before W2 panel reports.
- `complex_predictor_contract.py` is the W3 contract gate. If it blocks, downstream predictor commands
  must stay non-runnable; use `--emit-sync-back-plan` when the blocker is a missing secondary-record JSONL.
  That sync-back script validates its manifest and requires each pulled secondary-record JSONL to be
  non-empty before refreshing the contract.
- `complex_project_status.py --predictor-sync-back-plan results/m6c_second_predictor_sync_back.sh`
  preserves the W3 sync script path inside the top-level roadmap JSON/text and post-sync rerun command.
- `complex_project_status.py --emit-pending-external-paths results/m6c_project_pending_external_paths.txt`
  writes the combined W1/W2/W3/W4 external artifact checklist for sync/replay audits.
  The status JSON also includes `pending_external_summary`, grouping that checklist by workstream,
  category, target, artifact, and field before any remote-check report exists, and
  `pending_external_followups`, which maps that pre-remote checklist to W1-W4 repair actions.
- `complex_project_status.py --emit-target-msa-precompute-plan results/m6c_project_target_msa_precompute.sh`
  writes one deduplicated W1/W2 target-MSA precompute script from the raw scale/panel manifests. It keeps
  shared targets such as `1BRS_AD` to a single MSA job before the remote-check bridge only when their
  FASTA/MSA/report material matches; conflicting duplicate target ids fail closed as a local plan
  conflict before any submit or receipt write. The generated
  script requires `sbatch` before it initializes the receipt, so accidental local execution fails before
  clobbering resume evidence; set `TARGET_MSA_PRECOMPUTE_ALLOW_NO_SBATCH=1` only for explicit diagnostics.
- `complex_project_status.py --emit-external-remote-check-plan results/m6c_project_remote_check.sh`
  writes a lightweight `ssh test -s` preflight for that checklist, so missing or unfinished Cayuga jobs
  are detected before any local sync attempt. It also writes `results/m6c_project_remote_check.json`
  as machine-readable evidence of which remote artifacts were present or missing, including
  missing-by-workstream/category/target summaries for failed remote preflights. When a target-MSA
  precompute receipt path is known, it also tries to sync that receipt back and records
  `target_msa_precompute_receipt_sync`, including the synced local receipt SHA-256 and byte size; the pending
  external path-list fingerprint is checked before that receipt sync so stale remote-check scripts fail before
  local side effects.
- `complex_project_status.py --external-remote-check-report results/m6c_project_remote_check.json`
  consumes that evidence on status refresh. Only a fresh report matching the current pending-path SHA and
  `ok=true` advances `recommended_next_script` from remote check to external sync, and only when its
  `path_manifest` provenance matches the current pending-path manifest, its status/counters/per-path records
  prove every current path is present, and any required target-MSA precompute receipt is satisfied; fresh
  missing reports keep the remote-check bridge recommended and add
  fresh-report-derived `remote_missing_followups` for the relevant W1-W4 readiness/workstream repair action.
  Fresh all-present reports that lack target-MSA receipt-sync evidence are treated as
  `target_msa_receipt_sync_missing`, keeping the next action on remote-check instead of target-MSA re-submit.
  Reports where receipt sync was requested but did not sync the receipt are treated as
  `target_msa_receipt_sync_failed`, keeping the blocker explicit for Cayuga receipt repair. A digest-free
  `synced=true` receipt report is also treated as stale repair-required evidence. External sync scripts pin
  the satisfied local receipt SHA from status generation and recheck it before rsync.
- `complex_project_status.py --emit-external-sync-back-plan results/m6c_project_external_sync_back.sh`
  writes the one-command external pull bridge for that combined checklist. It leaves W3/W4 direct sync
  scripts as provenance comments and hands local reruns to `results/m6c_project_post_sync.sh`.
  Per-path `rsync` steps are failure-collected so later pulls and post-sync still run after a partial
  external-sync failure, while stale checklist fingerprints still fail closed before any rsync. When the
  remote-check plan is emitted too, this bridge also requires the matching all-present remote-check report
  and any required locally satisfied target-MSA receipt before it starts `rsync`.
- `complex_project_status.py --emit-post-sync-plan results/m6c_project_post_sync.sh` writes the local
  replay plan after sync. It reruns W1/W2 completion/readiness checks, reruns W3/W4 from captured
  `self_command` values when available, then refreshes project status. Each step is failure-collected
  so later checks still run after a partial sync, but the script exits nonzero if any replay step failed.
- Pending-path sidecar manifests record path-count and SHA-256 fingerprints; generated input-prep and
  external sync scripts check the current checklist before rsync and fail closed on stale path lists.
- Generated sync steps also verify that each local pulled file is non-empty immediately after `rsync`,
  before local replay tries to consume it.
- Bridge scripts derive the repo root from their own path and post-sync replay bootstraps
  `BIO_SFM_PYTHON` (falling back to `python3`), `PYTHONNOUSERSITE=1`, and local `PYTHONPATH` for fresh-shell
  reruns. Sync and remote-check bridge Python snippets also honor `BIO_SFM_PYTHON`, and post-sync replay
  checks its generated dependency manifest before rerunning local status commands.
- Generated bridge/status artifacts are written by atomic replace so post-sync refreshes can safely update
  bridge files even when a parent shell script is still running.
- `recommended_next_script` in `results/m6c_project_status.json` is the first bridge to run when resuming
  the goal; currently it should point to `bash results/m6c_project_target_msa_precompute.sh` when W1/W2
  target MSAs are pending, followed by `bash results/m6c_project_remote_check.sh`, a project-status refresh
  that consumes `results/m6c_project_remote_check.json`, then `bash results/m6c_project_external_sync_back.sh`
  only after the refreshed status recommends it.
- `target_msa_precompute_receipt` is the JSONL submit/reuse receipt for that first step. If it records
  exactly one accepted row per planned target as `submitted` or `validated_existing`, has no unexpected
  target rows, has a non-empty, whitespace-free `sbatch --parsable` job id for each `submitted` row, and has FASTA/MSA/report paths plus manifest path/hash/workstream provenance matching the current
  raw manifests, resumed status marks
  target-MSA precompute `satisfied` and recommends the remote-check bridge. The remote-check bridge can
  pull this receipt back from Cayuga so local status does not require a separate manual copy.
  A non-empty incomplete/invalid receipt blocks blind resubmission; the generated bridge verifies raw
  manifest hashes, helper file hashes, source target FASTA regenerability, and required `ENV_PY`/`BOLTZ`
  runtime executables before receipt initialization, and refuses to record a `submitted` row when
  `sbatch --parsable` returns an empty, whitespace-only, or whitespace-containing job id. On Cayuga, keep
  `ENV_PY` pointed at the Boltz conda Python or set `BIO_SFM_PYTHON` explicitly; the target-MSA bridge uses
  that env Python before falling back to `python`. The MSA helper also recovers a matching `.a3m` from an
  existing work directory or after a Boltz nonzero exit when the failure happens after MSA generation.
  Inspect or archive it first, or set
  `TARGET_MSA_PRECOMPUTE_OVERWRITE_RECEIPT=1` only after confirming recorded jobs will not be duplicated.
  It also self-validates the receipt before exiting: each rendered section requires exactly one accepted
  row for its expected target subset and exact FASTA/MSA/report path plus manifest/workstream provenance,
  and the project-level bridge finishes with a strict aggregate target-set/provenance check so unexpected
  or stale receipt rows are caught before the remote-check handoff.
- `target_msa_precompute_script_validation_audit` records that the emitted project bridge still contains
  those section-level, strict aggregate receipt, helper/source, and runtime guards. If this fails, goal mode
  treats it as a local blocker and regenerates/fixes the bridge before Cayuga submission.
- `generated_scripts[*].manifest` summarizes sync-script manifests (`n_paths`, SHA-256, source/sidecar)
  so resume audits can confirm which checklist each script was generated against.
- `sync_manifest_audit` must be `ok` before treating generated sync scripts as fresh; if it fails, rerun
  the relevant generator/status command instead of using the stale script. Failed audits mark the
  recommended sync script as blocked in status JSON/text.
- `resume_bridge_preflight` records whether the recommended bridge exists locally, whether any required
  pending path/manifest files are present, whether `sync_manifest_audit` is fresh, and whether the only remaining
  execution prerequisite is a missing env var such as `CAYUGA_BIO_SFM_ROOT` or a Cayuga-session-only submit
  step. `waiting_on_env` and `waiting_on_cayuga_session` are not
  regenerated-artifact blocker; export the env var, then run `recommended_next_script`. The same preflight
  records a non-executing `bash -n` syntax audit and marks syntax errors as structural blockers.
  For runnable/external-waiting bridges, preflight `next_action` also carries the downstream
  remote-check/status-refresh continuation when the resume ladder depends on it.
- `generated_script_syntax_audit` records that non-executing syntax audit for every generated bridge script,
  including later `resume_execution_ladder` steps that are not recommended yet. Syntax failures become
  `goal_progress_audit.local_blockers` so goal mode does not advance through a broken ladder.
- `resume_execution_ladder` orders generated bridge scripts plus the non-shell `project_status_refresh`
  pseudo-step into the expected continuation chain, for example target-MSA precompute -> remote check ->
  status refresh with `--external-remote-check-report` -> external sync-back -> post-sync replay, and marks each step
  as ready, waiting on env, waiting on remote jobs, blocked by a predecessor, or already satisfied.
- `pending_artifact_local_audit` records whether status-listed pending input/external artifacts are already
  present, empty, or missing in this checkout. If it says `all_present_nonempty`, run the relevant local
  replay/workstream checks before spending another sync attempt; if it says `all_missing`, the external
  bridge is still genuinely needed.
- `goal_progress_audit` maps the full Codex goal onto the W1-W4 requirement states, names the current
  first action, separates local from external blockers, and carries `can_mark_goal_complete`. Keep that
  guard false unless W1, W2, W3, and W4 are all complete in the current status artifact.
- `results/m6d_goal_completion_audit.{json,md}` is the standalone no-submit completion-boundary audit.
  The current honest state is `audit_ok=true` and `can_mark_goal_complete=false`, with W2 as the only
  remaining requirement until the v11 panel is explicitly approved, submitted, synced back, completed, and
  target-wise certified.
- `results/m6d_goal_drift_audit.{json,md}` is the standalone no-submit goal-boundary drift audit. The
  current honest state is `audit_ok=true`, `major_direction_drift=false`, direction aligned, claim
  boundaries preserved, and execution `panel_remote_readiness_ready_not_submitted`; next action is explicit
  W2 v11 panel approval only, followed by sync-back, completion, and target-wise certification.
- `results/m6d_local_cayuga_mirror_audit.{json,md}` is the standalone no-submit mirror audit. It compares
  exact SHA for stable handoff/source artifacts and semantic fields for generated JSONs that contain local
  or Cayuga paths; current status is `local_cayuga_mirror_agree` with 11 exact checks and 7 semantic checks.
- Top-level `goal_progress`, `remaining`, `remaining_requirements`, `can_mark_goal_complete`, and
  `goal_completion_note` mirror that compact completion/resume state for lightweight checks.
- `operator_next_action`, `operator_next_command`, and `operator_next_role` mirror the goal-audit first
  action at top level; `operator_next_action` also carries the required downstream remote-check/status-refresh
  continuation when a ladder step depends on it. Treat them as the resume instruction; `next_action` remains the scientific
  workstream-level action.
- `run_batch_round.py` is the W4 DBTL gate. If batch JSONLs are missing or empty, use
  `--emit-sync-back-plan results/m6c_w4_sync_back.sh` so the preflight blockers become a replayable
  sync-back/rerun script. For calibrated routing, its `gate_prevalidation.batch_contract` must show that
  prior records and current batch records share the same predictor/source/label-threshold contract by regime.
- `complex_readiness.py --batch-sync-back-plan results/m6c_w4_sync_back.sh` preserves that W4 sync
  script path inside readiness JSON/shell artifacts.
- `complex_project_status.py --batch-sync-back-plan results/m6c_w4_sync_back.sh` preserves that W4 sync
  script path inside the top-level roadmap JSON/text and post-sync rerun command.
- W3/W4 direct sync scripts validate their own `<script>.manifest.json` sidecars before rsync, then require
  each pulled JSONL to be non-empty before refreshing or rerunning the workstream. Their direct replay
  commands avoid regenerating the running sync script.
- `complex_project_status.py` is the roadmap dashboard. Refresh it after every meaningful analysis or
  completion artifact. The status JSON includes `self_command`; use it as the exact current refresh command
  when resuming, then compare `sync_manifest_audit`, `resume_bridge_preflight`, and
  `pending_artifact_local_audit` before running any generated bridge.

## Verification Policy

- For code changes, run focused tests for the touched tool and then broader tests if behavior crosses
  workstream boundaries.
- For docs-only changes, at minimum run `git diff --check`.
- For generated workflow artifacts, rerun the generator and inspect the emitted replay command.
- Before reporting a local step as done, state:
  - W1/W2/W3/W4 status if relevant;
  - `goal_progress_audit.status` if present;
  - the next concrete command;
  - verification performed;
  - whether the remaining blocker is local or external.

Known good full verification commands:

```sh
/tmp/bio_sfm_science_venv/bin/python -m pytest -q    # current local WIP harness: 676 passed
/tmp/bio_sfm_venv/bin/python -m unittest discover -s tests
/tmp/bio_sfm_venv/bin/python -m unittest discover -s ../bio-sfm-trust-core/tests
/tmp/bio_sfm_venv/bin/python -m compileall -q src hpc tests
PYTHONPATH=src /tmp/bio_sfm_venv/bin/python -m bio_sfm_designer.experiments.public_release_readiness --repo-visibility public --check-git-status
git diff --check
```

## Turn-End Template

End each goal-mode turn with this compact state:

```text
W1:
W2:
W3:
W4:
Goal audit:
Next command:
Verification:
Remaining blocker:
```

Keep the summary short. The durable state belongs in JSON artifacts, runbooks, and this repository, not
only in chat.
